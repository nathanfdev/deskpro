These configs have pre-set values suitable for development.

1) You still need to edit config.database.php to input your db info.

2) You still need to edit config.paths.php to input paths to required binaries.