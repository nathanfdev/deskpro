#!/bin/bash

DIR_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/../.."

echo ">> Cleaning node_modules"
rm -rf $DIR_ROOT/www/BUILD/pub/node_modules
echo ".. done"

echo ">> Installing dependencies"
cd $DIR_ROOT/www/BUILD/pub
yarn install
echo ".. done"
