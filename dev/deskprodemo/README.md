These config files are appended to the config files on on demo sites (https://builder.deskprodemo.com/).

This can be helpful in creating easily QA'able builds that require specific configuration or access to third-party
services / APIs that need settings such as API keys.

NOTE: Obviously these files are readable by anyone who can read the repository, so you shouldn't put anything too
secret in here.
