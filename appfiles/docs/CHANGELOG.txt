################################################################################
# Alpha 3
################################################################################

* User order by

* Cron step

* Changes to custom fields logged in ticket log

* Handle attachment upload errors in userland

* Fix empty team icons (until we have ui for uploading)

* Fix JS error

* Add config override for a user password (ie way to reset admin password)

* Add dp logo to login screen

* Fix label

* Dont show "default" if currently editing default

* Dont show message types notes/attach when no messages of that type

* Show error on invalid email, catch duplicate emails

* Revert that, was something else

* Fix style of reset link pos

* Remove old field handlers, fix reordering of choice options

* Fix setting initial email address on agent, email them welcome email

* Fix preselected usergroup

* Few fixes of installer

* Fix z-index of menu so it overlaps the disabled screen overlay

* Tabindex so tabbing from pass field doesnt select reset link

* Attempt to create empty database if error

* Explain memory limit check

* Better log messages in verify files list

* Fix cache paths in install kernel

* Remove new deal related things

* Make obvious how to add new things

* Default install data

* Attach sidebar to side of faux browser window again

* Fix sizing/scrolling bugs with editor while you enable and disable stuff

* Ideas on by default

* Add home button to admin nav

* Install default agent teams, and the 'all permissions' agent group

* Have each kernel share a common annotations, doctrine proxies and twig compiled directory

* Redirect back to field listing after editing custom field

* Fix sql error

* Fix a few couple php notices

* AdminKernel is separate now too

* Install kernel needs to use the custom cache warmer too

* Update path

* Changes to prod build for some changes in updated symfony

* Interface switcher icons

* Use dpcheckbox in filter show slideout, easier to align than native checkbox

* Fix extra </div> in scroll container macro

* Get scrolltop working with the custom scrollbar

* Dont show interfaces list in the interface switcher if you dont have permission to see it or if its disabled

* Auto-size rte when editing articles, fix weird padding on content

* Fix tree indentation past two levels in optionbox

* Keep track of total user waiting time

* Fix position of arrow on button

* Fix news and downloads results in quicksearch

* Fix url to reports interface

* Fix some style issues with borders on control bars

* Work around custom fields and collections, and rendering custom field values on filter results

* Show all fields with a value on viewticket, and all form fields when editing

* Build time sholudnt be included in sources

* Fix status message

* More minor fixes to reporting

* Get rid of YAML, use PHP configuration files

* Add simple one-line note to listing pages when you haven't defiend any of that particular thing yet (instead of an empty table)

* When adding the first of something, make sure to toggle on the feature too

* Make sure the feature togglers work to turn on/off stuff

* Make sure the use_xxx settings that are used to determine which features are enabled are set properly when deleting objects. For example, when the last agent team is used, then the use_agent_team setting should be turned off.

* Fix auto-redirection to license page after install, sholud go to admin welcome first

* Exception when no config for a database in ConnectionFactory

* Fix using raw assets when not in debug mode

* Fix CLI with new config structure

* Fix full build scripts with new config structure

* Inline sortable for display options

* Sync counts in tasks if you reassign

* Show details on the 'problem loading the tab' error message

* Prettier login screens

* Fix pager not carrying over display fields, add new pager to the fullscreen ticket listing overlay

* Fix php warning

* Search on email domains, put people results first when doing an email search

* Dont show new comment button either

* Dont show new comment on completed tasks

* Arrow placement

* Fix department search on '0'

* Some cleanup to the publish section listings

* Fix slowness of expanding "other" section in New* overlays

* Fix autoloading routes on lists sometimes, properly load the 'new' list on Ideas

* Make size of message boxes based on the height of the window

* Add setting to control if using agent teams, products, ticket categories, ticket priorities and ticket workflows. In templates, hide relevant sections if the field is disabled

* Fixes and tweaks to filter preferences slideout

* Fix space between border on selected

* Improvements to publish layout

* Save status of admin setup tasks and update the progress bar

* Header tasks thing

* Get redirects and step storing in place

* Quick setup page

* Filters causing assetic less compile to fail

* Simple hint boxe style to team/agents

* Show full form for create agent

* Permission dependency processing

* Get rid of icon for agent zone in listing

* Fixes to handling default gmail transport

* Changes to add gateway account

* New layout for portal editor

* Implement system of storing blocks and their data, display order, state

* More portal fixes and design tweaks

* Fix few more portal bugs

* Some tweaks and fixes to portal

* Editor preloader, add new standard header, move colorwheel icon

* Filter display tweaks

* Getting filter pages up to date

* Rest of updates

* Similar to ticket cats

* Move fields menu item above editor

* Similar changes to products

* Update listing of fields, and ticket properties

* Delete icon, proper delete counts, when deleting tree warn that it deletes children too, move chats when deleting too

* Drag handles on rows on mouse over

* New listing layout, update drag+drop to work with new layout, new and edit as overlays

* Change pagenav position calculations to handle when the screen is smaller

* Update embed code snippet, move embed code into overlay, allow multiple 'pagenav' blocks to exist on a page

* Some updates to get iframe working, some fixes to newticket email with validation enabled

* Tweak nav/title

* Auto-save

* Add drag lines

* Add toggle for new ticket layout too, sort out order of fallback layouts for when custom layouts dont exist

* Fix rendering form fields on 'view' layout when items are drawn from dragging

* Dont show form fields on view list since its not a form

* Implement toggle for custom dep layouts, add button to revert to default, add on/off toggle for view/modify zones

* Invalid page handler

* Some getByIds collisions

* Shouldnt be suppressing errors here

* Always send errors to error_log so server can log errors

* Remove getByIds in Ticket repos, it's defined in the asbtract repos now

* When building, attempt to deny access to web interface so cache files arent written simultaneously

* Fix label autocomplete url error

* Fix typo in getEl

* Implement search bar with first 'quick search' results, keyboard navigation, styling and key behavior, hooks ready for second more intensive searchengine search

* Work around quick search

* Toggle for per-department fields

* Another filter problem

* Fix less error to do with filter:

* Update to the ticket editor layout

* Fix padding around agent list sidebar, add '+' icon to add buttons

* Changes to the agent layout / sidebar boxes

* Modify sidebar'd layout to be in a center white area

* Fix text alignment of options, autofill test dialog with default from, fix toggling backup not hiding a previously selected option when re-enabling.

* Fix PHP error to do with gmail options, fix misplaced 'test' button, fix toggling backup on/off not properly showing row highlight on backup form group

* Layout to triggers page

* Layout to other pages

* New style changes, fix turning off backup mail server

* Save zone access properly, add simple icons for them (agent/admin/reports/billing)

* Add delete for gateway accounts and transports, when adding gmail account automatically add smtp for it too

* Some more on the search bar ui. The option box builder converts a normal select box into an option box widget, and then displays the values neatly

* Get structure of search

* Change loader into pagination

* Fix going over 1000 pagination / incorrect pagination count

* Work around image/attachment storing:

* Add install check for GD or some other image manip extension, add service for Imagine

* Add Imagine sources

* Add Imagine to vendor tracking

* Attachment uploading to notes

* Change title of display options, tweak style a bit

* Add signature to replybox, handle inserting snippets properly around the signature

* Fix cloud tab not working on ticket section

* Keep list counters up to date with publish and ideas section too

* Keep UI counts/elements updated as lists change and filters are updated in real time

* Fix setting "none" group not saving preference

* Rename massactions 'widget' to just MassActions

* Save selected grouping preferences

* Remove icons for twitter/deals

* Use defines for config values

* Fix a few issues in IE

* Log 404's in a separate log, add basic error pages with SN codes for production mode, if a SN code isn't provided (ie some fatals), then the error popup in agent shows the raw output from the server in a box

* Normalize zIndex and use saner values

* Move tinymce stuff into a central DP helper method, and change fullscreen mode to within the window to fix browser issues

* Fix dupe department names

* Fix some issues with popovers and scrollable inner areas due to footers

* Remember email when failed login, and show error message

* Fix updating password and auth check

* Fix selection issue on newticket department, selecting a child enters full name in rendered text spot

* Fix browser hashes that have been URL encoded

* Fix a sticky nav color with hash loading

* Fix grouping issues, fix on hold issues, fix filter criteria for hold items, fix selected nav item color

* Tweak display of avatar so it sits on white background

* Fix close icon bleeding on agent chat window

* Fix opening new agent chat

* Dont allow saving of empty filters

* Dont make your own agent name clickable to chat

* Fix bug with hydration and order of operations from last commit

* Ticket log for adding and removing participants, work in the filter detector and list updater to properly handle filters that are based on participants

* Fix php warningsin quick user search dropdown

* Stick selected nav item when clicking it in ticket section

* Fix showing both "none" and "unassigned" nav rows for agent and agent team

* Fix initial display of 'click to show filters' text

* Test for valid callback and report if its invalid

* Fix for when a popover load is in the background via a timer, but is destroyed before the load happens

* Double destroy fix

* Fix routing error when trying to load a ticket that wasnt created

* Fix some bugs with destroying popovers, and destroying them before the page is loaded

* Fix missing search adapter error

* Dont allow rating of your own messages (ie agent using user interface)

* Number of CSS/positioning fixes

* Fix issue with updating views and sometimes $searcher not being defined but its needed for generating summary used in templates

* Fix hidden error when grouping ticket list results

* Disable filesystem refreshing of compiled templates when in prod mode

* Fix version check on old PHP versions due to __DIR__ being new in PHP 5.3, and syntax errors in boot_* files due to use of namespace separator '\'

* Fix email address of notificaiton

* Fix error with validate email notification

* Fix unknown variable error

* Add placeholder favicon, fix 404 on css image

* Fix bug with agent chat sometimes causing error with detecting existing recent conversations

* Fix references to some user routes in reporting

* Error reporting to us: add referral url, add user agent, fix request data in

* Check memory limit on install, attempt to set min memory limit and max exec time on preboot

* When reporting errors, submit the build time of the package too

* Check for mbstring in installer

* Add some more files to be removed in build vendors

* Update vendor files

* Update vendors:

* Set default from address in actions

* Fix updating of default email address

* Few tweaks with the gateway reading to add debug lines, fix missing user notification, and order of operations on trigger executions

* Update department page with new permission schemes

* Fix ticket terms not using proper option var in description for category types

* Move around and remove nonsense options depending on trigger, add some missing people options

* Fix bug in Util::callUserConstructorArray

* Updated layout of rules, add drag handle, fix some rules, add stop processing rule

* Add order to triggers, add collection modifier that stops further triggers from running, give actions and terms a "description" so they can be rendered to text, add drag and drop to listing

* Redirect /tech/ to /agent/ and /admincp/ to /admin/

* Tweaks to user reg settings page, fix bug in ticket editor that caused error

* Update usergroup editing and listing

* Simple page to switch from open/closed auth/closed

* Remove 'settings' link for ideas and others which don't have anything yet

* Fix undefined constant 'id' error

* Few changes to new trigger ui, add criteria for gateway address, add action rule to set "from" address on notifications

* Simplified trigger listing ui

* Separate default outgoing email into Setup, add single 'email gateways' under Tickets, show section for outgoing email accounts in sidebar on gateways page

* Typo

* Cleanup of an old method call

* Fix agent notifications for new tickets

* Update/fix a number of gateway related tasks, add gateway addresses and address matching

* Get console command working through index.php loader

* Add toggler button to accounts

* Merge branch 'master' of https://github.com/DeskPRO/DeskPRO

* Fixes to upgrader

* Managing of gateway connections

* Save a log of messages even when they reportedly send properly, add cleanup cron job, if a send fails then keep trying but increase time between attempts each time

* Tweak form a bit for better input handling

* Fix maxlength on pattern field

* Fix DelegatingTransport to start connections if they need to be since our mailer overrides that bit from swiftmailer

* Add gmail type, add ability to force a specific transport on specific messages, add ability to test email settings

* Add new email transport handling / rules

* remove exit() debug

* Merge branch 'master' of https://github.com/DeskPRO/DeskPRO

* lots of fixes to old upgrader

* Make iframe background transparent so page bg comes through

* Some embedding options, script that embeds into any html page

* Dupe validation options for agents

* Update display of validation options, add option to add css classname

* Add old upgrader to sources

* Get ready for different formats

* Add subject and department as field items, make subject, department and message irremovable

* Use optiobox for department switcher

* More prominent message about

* Add loading icon to saving editor form

* Fix width of subject/message field

* Fix navigating to different sections and the department switcher

* Fix implode warning on summary

* Show department before form if you dont have JS enabled

* Split up templates, cleaner wrapper elements

* Save per-field rules on ticket editor and handle redrawing them, implement those rules in user interface

* Ticket layouts in userland

* Add message and attachment draggables, fix on/off switch for department layouts

* Updated ticket editor to add on/off switcher, different forms for new/view/modify

* Make sure to build InstallKernel caches as well

* Fix invalid template names in reset password form

* Fix ticket editor choice field

* Fix other jumping tabs problem

* Revert to default

* Saving ticket editor, redrawing it when coming back

* Streamline cli/cron booting

* Fix JS error in Popovers when they're destroyed but their reload timer is still active

* Update changelog












################################################################################
# Alpha 2
################################################################################

* Fix preselected values in form

* Fix setting choice fields

* IE fixes

* Fix comment methods on Task comments

* Dont compress color values or else when they're used in IE's filters they go crazy

* Try to solve IE JS error on newticket

* Install keeps track of which queries its done

* Update changelog

* Always force index.php

* Catch invalid database name or invalid credentials while attempting to boot, and show it as an install error page

* Fix notices in chat session starter

* Fix installer on chrome

* Add changelog

* Fix jumping tabs problem

* Fix ticket assignment

* Tweak positioning of arrow a bit

* Fix JS error when debug disabled

* Create/update build time file before checksum file is generated

* Remove hidden status type, not used

* Remove person tooltip

* Fix log proxy giving errors in IE

* Fix alignment of checkbox/selection count in listbar on Windows

* remove test debug comments

* New ticket valdiation

* Dev message in omnibox

* Fix clearing issue on people list, some margins

* Fix close menu on empty space

* Show coming soon message on twitter and deals

* Show listview box with loading spinner while list loads

* Fix messed up alighment on vieworg

* The arrow itself becomes a close trigger

* Fix extra h-line on first row of new forms

* Fix weird new org pop

* Fix RTE menus and remove path: bits

* Fix weirdness with buttons
- Firefox freaks out with positioned elements inside a button el, so if there is an arrow must use an anchor instead

* Visual feedback on applying ticket actions

* Number of changes to Http client in ZF2 update

* Fix jumping avatar when opening profile menu

* Fix weirdo arrow icon

* Tweak style of ticket grouping editor a bit

* Fix merging and ticket log

* Clean up handful of validating comment related things

* Fix popover positioning in firefox due to visibility detection applied earlier

* Fix for APC bug with customs session handlers (see PHP bug #58739)

* Fix selected state of idea nav elements

* Fix going back to "all" when viewing a grouped ticket list, and fix counts/pagination when viewing a grouped list

* Fix scrollbar issue

* Fix console proxy in chrome

* Clean up new person popup using same org widget from viewperson

* Bind focus to create windows (fixes tab index) when they're opened, standardize footers of create windows, standardize save button to "Create x", add standard keyboard shortcut alt+c to submit forms

* Count for 'active chats' row

* Dont show x of y or 'show more' when no results, dont show 'show more' when already showing full result set

* Hide label display type switcher when no labels, fix switcher tabs on tickets section

* Fix flagged counts, and keeping them updated as you change flags

* Fix rendering comments as raw html, fix basic xss filter logic that determines when it needs to be run on input

* Fix on/off checkboxes in admin

* Fix notice with bad lang pack name

* Fix logout across different session types, actually remove session records, redirect back to originating interface with confirmation message

* Chnage tasks badge count to 'all my tasks'

* Save the #anchor when saving JS error reports

* Fix merge log error

* Fix message with no text snippets defined

* Fix deleting the first category!

* Insert initial categories on install

* Setting up agent lets you set password, correctly sets the is_user flag, and has basic error checking

* Fix bad phrase for created date in user interface

* Fix filter update event not being handled (aka list counters not updating after a change)

* Fix custom filters hide/show editor beign triggered by "no filters" message

* People list view

* Fix dreadful blue IE gradients

* Redirect sections without the trailing slash instead of erroring

* Create a build time, and use that to cache break with assetic resources

* Fix weird person profile ui when no custom fields

* Fix not opening new person if you didnt also give them an org

* Update actual symfony and zend2 files

* Update cleanup method for latest zend2 checkout

* Update Feed reader to support newest zend2 additions

* Update symfony and zend versions in vendor build script

* Update mootools to 1.4.2

* Work with the omnisearch box

* Fix $ticket_deleted error

* Delete and ignore generated schema file

- minor style update for config file

* Remove ContentSearch entity

* Fix schema file with extra table

* Fix checksum check with static (or anything outside of appfiles/)

* Send js error logs

* Show KB section even when empty

* Slightly bigger font on massactions select, bit more spacing around them

* Fix no hash for app/list when no tabs, and js error when hash had no tab descriptions

* Improve wording of unassigned

* Fix some missing file loading problems

* Fix static files not being checked with checksum verifier

* Get proper queue system working with mysql-powered default, add fulltext search table for mysql-powered search, get proper entity listeners ready to automatically update search indexes when objects of interest change

* Update layout of chat section

* Fix labels in filter builder

* Fix button placement

* Remove call to PIE.htc

* Set default

* Add DP_DEBUG constant definition to other interfaces

* Add new error log writer that posts data to our server for saving

* Fix preselected 'me' instead of unassigned

* Arrow keys and enter key with the person selector

* Do console logging through DP.console

* Add DP object

* Bit of a play with query cache

* Fix bug with expire caches

* Fix setting ticket on hold

* Shrink width of bg image

* Updated publish section with collapsible areas

* Deals badge

* Rename Deal.js to Deals.js

* Badges on ideas/publish icons, and some other count continuity fixes

* Kill off another load query to fetch online agents

* These messages were made implicit, no need to subscribe and send a request

* Kill off another ajax request by inlining ticket labels section

* Get rid of another ajax request by removing need to subscribe to more always-on channels

* Load all section data at once, turns 9 ajax requests into 1

* Fix wrong url to admin

* No cat title when no categories exist

* Restyle regsiter page

* Typo

* Fix user css, fine-tune routing resources

* Fix org box icon placement

* Fix url to label search

* Fix error to do with bad url

* Fix more bad image paths

* Tweak colorbox theme a bit and put css for it into dp-interface (fixes bad path too)
- Fix wrong path to mag icon in omnisearch

* Icons for away status

* Small icon for content types

* Fix many visitors being created due to cookie path, carry over admin sessions to agent and reports

* Handle when only one dep

* Fix misc errors

* Task badge count

* Fix client messages sending when assigned task changes (and fixes php notice)

* No labels messages

* More cleanup

* Some cleanup of old routes and methods

* Updates to organization listings

"No more results" message

* Fix clicking on "no online" agents bringing up weirdo chat box

* Show error when upload fails

* Show message when there are no snippets defined yet

* Dont show ajax error on ErrorLogger saving some other error

* Fix when SimpleTabs are used on a container with no tabs yet

* Remove subscription calls to channels everyone is always interested in (saves an ajax request on load)

* Fix debug override

* Remove template/type chooser

* Add editing timezone with 'edit mode' of custom fields, showing session info about landing page and referrer

* Show message about creating new filters when there are none

* Reload labels sections when they're edited from a person or org

* More work on better js errors

* Remove test email file

* Fix capitalization

* Update twig vendor files to 1.4

* Update twig from 1.3 to 1.4

* Make tabbar same width as fields

* Dont show category tabs if none

* Fix missing close button on some overlays

* Dont show teams tab if none, add missing phrases

* Missing phrase and typo

* Show current picture
- Will always show gravatar if none, and default picture if no gravatar

* Typos

* Improve user icon

* Fix ugly font on logout button

* Add backdrop on settings wind

* No labels message

* Standardize capitalization

* Make flags a bit smaller and fix weirdness in ui

* Agent chat fixes
- Fix huge default avatars on button
- Fix minimize
- Fix closing chatbox on document click
- Add hover images for close/min buttons

* Fix typo bug in zend framework 2

* Fix redirect loop round 2

* Fix only one task being saved on multi-task adding

* Fix license infinite redirect

* Fix setting changed flag when it shouldnt on verify files step

* Force index.php in URL

* Improve install redirect when config is detected but is default values, or settings table doesnt exist

* Fix whitepage on no config check

* Enable full error info by default so it's available preboot

* Dont include classes.map

* Add safe mode check, change preboot checks so more than one message can be displayed at a time if multiple checks fail

@ suppress ob_flush()

* Clean html bodies

* Add HtmlPurifier cleaner type for html, simple_html and html_email

* Add vendor build process for html purifier, add it to autoloading

* Add HTMLPurifier

* Enable input filtering of malformed utf8, enable filtering of bad 'invisible' characters, and enable basic xss filter based on codeigniters xss_clean

* Fix browser hashes

* Handle errors when opening new tabs to try and prevent 'phantom' tabs

* Log JS errors if possible

* Clean up the sys bundle and kernel and routing

* Add a stacktrace helper

* Dont assign system objects to vars, instead fetch them with magic so they can be lazy-initiated through the DI

* Fix timeout exception handling

* Cleanup a bunch of other old files and controllers

* Remove old form component from orb

* Separate out admin and agent kernels from eachother, and differentiate routing loaded for each (they dont need to know eachothers routing, makes them slower)

* Fix varname in debug.php

* Fix redirect to install on detecting missing table, fix installer being in debug mode

* File checksum in installer

* Clean up config.php, remove config line for database driver since it's always mysql

* Update readme

* Update build-vendors to get rid of zend stuff we dont need

* Remove unneeded zend stuff

* Trim down ezC

* Change zend1 to zend2

* Remove elasticabundle from submodules

* Fix some vendor builds

* Remove zend1

* Add monolog

* Adding vendors as files

* Remove some fixture descriptions from doctrine

* Remove .gitignore from metadata

* Remove vendors as submodules, add script to build vendors

* Remove submodules

* Move debug and assets config into sys config dir

* Remove old js-sources and command

* Fix .gitignore

* Build checksums

* Changes to config location, install loading, booting and kernel sources

* Install key compoent to licenses

* Disable sql mode on connect

* Fix long description
