<div class="dp-idea-vote {% if has_voted_ids[idea.id] %}dp-voted{% endif %}" data-vote-url="{{ path('user_ideas_vote', {'idea_id': idea.id}) }}">
	<span></span>
	<em>{{ idea.total_rating|default(0) }}</em>
</div>
