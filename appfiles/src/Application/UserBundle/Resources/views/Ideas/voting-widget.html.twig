<div class="dp-feedback-vote {% if has_voted_ids[feedback.id] or has_voted_this %}dp-voted{% endif %}" data-vote-url="{{ path('user_feedback_vote', {'feedback_id': feedback.id}) }}">
	<span></span>
	<em>{{ feedback.total_rating|default(0) }}</em>
</div>
