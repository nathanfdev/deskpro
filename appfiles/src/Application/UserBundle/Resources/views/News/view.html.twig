{% extends 'UserBundle::layout.html.twig' %}
{% block content %}
<div style="float:left; width: 190px;">
	{% include 'UserBundle:News:category-list.html.twig' %}
</div>

<div style="margin-left:200px;">
	<div style="float:right">
		{{post.date_created|date('Y-m-d')}}
	</div>
	<h4>{{post.title}}</h4>

	{{post.content_html|raw}}
</div>

<br style="clear:both" />

<div style="background-color: #eee; padding: 10px; margin-top: 15px;">
	Name: <input type="text" name="name" /> (public)<br />
	Email: <input type="text" name="email" /> (private, never revealed)<br />
	<textarea name="comment" cols="80" rows="5"></textarea>

	<hr />

	{% for comment in comments %}
		<div style="background-color: #fff; padding: 6px; margin: 10px;">
			{{comment.content_html}}
		</div>
	{% endfor %}
</div>

{% endblock %}