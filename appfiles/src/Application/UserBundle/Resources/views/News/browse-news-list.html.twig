{% for news in news_entries %}
	<li>
		<h4 style="margin-bottom: 0"><a href="{{ path('user_news_view', {'slug': news.url_slug}) }}">{{ news.title }}</a></h4>
		<div class="meta">
			{{ news.date_created|date('M d') }} &middot; {{ news.person.display_name }}
		</div>
	</li>
{% endfor %}
