{% extends is_partial_request() ? 'UserBundle::layout-partial.html.twig' : 'UserBundle::layout.html.twig' %}
{% block content %}
<div class="dp-content-block portal_kb with-handler" {% if is_partial_request() == 'portal' %}data-element-handler="DeskPRO.User.ElementHandler.PortalDownloads"{% endif %}>
	<div class="dp-box">
		<div class="dp-box-header">
			{% if category %}
				<h1>Downloads: {{category.title}}</h1>
			{% else %}
				<h1>Downloads</h1>
			{% endif %}
		</div>

		<div class="dp-box-content-wrapper">
			{% block downloads_browse %}{% endblock %}
		</div>
	</div>
</div>
{% endblock %}
