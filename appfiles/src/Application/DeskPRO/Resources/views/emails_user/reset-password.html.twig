{% extends 'DeskPRO:emails_user:layout.html.twig' %}
{% import 'DeskPRO:emails_user:layout-macros.html.twig' as layout %}
{% block content %}
	{{ layout.section_start() }}
		You request a password reset link. To reset your password, click the link below:<br />
		<a href="{{ url_full('user_login_resetpass_newpass', {'code': code}) }}">
			{{ url_display('user_login_resetpass_newpass', {'code': code}) }}
		</a>

		<br /><br />

		Your password reset code is {{ code }}
	{{ layout.section_end() }}
{% endblock %}