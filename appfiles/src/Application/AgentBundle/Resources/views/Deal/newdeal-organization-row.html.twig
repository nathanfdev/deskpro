<div class="col">
	<h4>Name</h4>
	{% if organization.id %}
		{{ organization.name }}
	{% else %}
		<input type="text" name="newdeal[person][name]" class="name" value="{{ organization.name }}" />
	{% endif %}
</div>
<br class="clear" />
