<table class="list revision-compare-table" width="100%">
	{% for rev in news.revisions %}
		<tr class="rev-{{rev.id}}">
			<td width="30">{{ loop.index }}</td>
			<td>
				<span style="float:right">
					{{ rev.date_created|date('Y-m-d') }}
				</span>
				By {{ rev.person.display_name }}
			</td>
			<td width="150">
				<input type="checkbox" class="rev-compare-check" value="{{rev.id}}" />
			</td>
		</tr>
	{% endfor %}
</table>
<div style="text-align: right">
	<button class="dp-button compare-trigger">Compare Selected</button>
</div>
