<section class="search-reuslts page-set" data-page="{{ page }}">
	{% for article in results %}
		<article
			class="row-item article article-{{article.id}} articles-{{article.id}} related-is-linkable"
			data-content-type="articles"
			data-content-id="{{ article.id }}"
			data-route="page:{{ path('agent_kb_article', {'article_id': article.id}) }}"
			data-route-title="@selector(a.row-title)"
			data-route-openclass="open"
		>
			<input class="item-select" type="checkbox" value="{{article.id}}" />
			<div class="top-row">
				<div class="top-row-left">
					<h3>
						<a class="row-title">{{ article.title }}</a>
					</h3>
				</div>
				<div class="top-row-right visible-ticket-controls">
					<div class="bound-faded"></div>
					<button class="clean-gray xxx-small kb-insert-link" data-article-id="{{ article.id }}">Insert Link</button>
					<button class="clean-gray xxx-small kb-insert-content" data-article-id="{{ article.id }}">Insert Content</button>
				</div>
				<div class="top-row-right visible-related-content-controls">
					<div class="bound-faded"></div>
					<button class="clean-gray xxx-small related-link">Add as related</button>
					<button class="clean-gray xxx-small related-unlink">Remove as related</button>
				</div>
			</div>
			<hr class="clear-top" />
			{% if display_fields|length %}
				<div class="extra-fields">
					<ul>
						{% for field in display_fields %}
							{% if field == 'category' %}
								<li>
									<span class="prop-title">Category</span>
									<span class="prop-val">{{ article.category_names }}</span>
								</li>
							{% elseif field == 'author' %}
								<li>
									<span class="prop-title">Author</span>
									<span class="prop-val">{{ article.by_line }}</span>
								</li>
							{% elseif field == 'date_created' %}
								<li>
									<span class="prop-title">Date Created</span>
									<span class="prop-val">{{ article.date_created|date('M j, Y')}}</span>
								</li>
							{% elseif field == 'view_count' %}
								<li>
									<span class="prop-title">View Count</span>
									<span class="prop-val">{{ article.view_count|default(0) }}</span>
								</li>
							{% endif %}
						{% endfor %}
					</ul>
					<br class="clear" />
				</div>
			{% else %}
				<br class="clear" />
			{% endif %}
		</article>
	{% endfor %}
</section>
