<script>
pageMeta.fragmentClass = 'DeskPRO.Agent.PageFragment.ListPane.IdeaFilter';
pageMeta.pageUrl = '{{ path('agent_ideas_filter') }}?partial=1&cache_id={{cache_id}}&page=$page';
pageMeta.refreshUrl = '{{ path('agent_ideas_filter') }}?cache_id={{cache_id}}';
pageMeta.saveListPrefsUrl = '{{ path('agent_ideas_filter') }}';
{% if cache_id %}
	pageMeta.cache_id = {{cache_id}};
{% endif %}
{% if not ideas %}
	pageMeta.noResults = true;
{% endif %}
</script>
<header class="section-header">
	<h1>
		{% if page_title %}
			{{ page_title }}
		{% else %}
			Ideas
		{% endif %}
	</h1>
</header>
<div class="layout-content">
<div class="content">
	<section class="ideas-simple-list deskpro-results-list">
		{% include 'AgentBundle:Ideas:filter-list-part.html.twig' %}
	</section>

	<div class="loading-more">
		Loading more results ...
	</div>
	<div class="no-more-results">
		There are no more results.
	</div>
</div>
</div>