
<ul class="nav-list">
	<li id="userchat_navitem_0" class="auto-load-route" data-route="listpane:{{ path('agent_userchat_listopen', {'agent_id': '0'}) }}">
		<span class="list-counter">0</span>
		Unassigned
	</li>
	{% for id,name in agent_names %}
		<li id="userchat_navitem_{{id}}" class="agent-chat-count with-route" data-route="listpane:{{ path('agent_userchat_listopen', {'agent_id': id}) }}" style="display:none">
			<span class="list-counter">0</span>
			{{ name }}
		</li>
	{% endfor %}
</ul>
