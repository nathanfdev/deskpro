<script>
pageMeta.fragmentClass = 'DeskPRO.Agent.PageFragment.Page.NewTicket';
pageMeta.title = 'New Ticket';
pageMeta.ticket_id = 0;

pageMeta.labelsAutocompleteUrl        = '{{ path('agent_ajax_labels_autocomplete', {'label_type': 'ticket'}) }}';
pageMeta.uploadAttachUrl              = '{{ path('agent_accept_upload') }}';

{% set baseId = el_uid() %}
pageMeta.baseId = '{{ baseId }}';
</script>
{# scroll containers #}
<div class="layout-content with-scrollbar page-new-ticket">
<div class="scrollbar"><div class="track"><div class="thumb"><div class="end"></div></div></div></div>
<div class="scroll-viewport"><div class="scroll-content">
{# /scroll containers #}

<form id="{{baseId}}_newticket">
<input type="hidden" name="newticket[person][id]" value="{{ person.id }}" id="{{baseId}}_person_id"/>

<div class="section user-section" id="{{baseId}}_user_section">
	<div class="step">
		<span class="num">1</span>
		<span class="check">Done</span>
	</div>
	<div class="content">
		<h4>User</h4>
		<input type="text" name="newticket[person][email_address]" id="{{baseId}}_usersearch" class="user-search" /> <button class="clean-gray xx-small" id="{{baseId}}_me_btn">Me</button>
		<div class="user-info" id="{{baseId}}_userinfo" style="display: none"></div>
		<div class="user-info new" id="{{baseId}}_guest_userinfo" style="display: none"></div>

		<br class="clear" />
	</div>
</div>

<div class="section department-section" id="{{baseId}}_dep_section">
	<div class="step">
		<span class="num">2</span>
		<span class="check">Done</span>
	</div>
	<div class="content">
		<div class="col">
			<h4>Department</h4>
			{% include 'AgentBundle:Common:select-department.html.twig' with {
				'name': "newticket[department_id]",
				'id': baseId ~ '_dep',
				'departments': ticket_options.departments_hierarchy,
				'with_blank': true
			} %}
		</div>
		<div class="col">
			<h4 class="light">Status</h4>
			<select name="newticket[status]">
				<option value="open" selected="selected">Awaiting agent</option>
				<option value="pending">Awaiting user</option>
				<option value="resolved">Resolved</option>
			</select>
		</div>
		<div class="col">
			<h4 class="light">Agent</h4>
			<button class="clean-gray xxx-small" id="{{baseId}}_assign_agent_choose"><span class="selected-agent" id="{{baseId}}_assigned_agent">Me</span></button>
			<input type="hidden" name="newticket[agent_id]" value="{{ app.user.id }}" id="{{baseId}}_agent_id" />
		</div>
		<div class="col">
			<h4 class="light">Agent Team</h4>
			{% include 'AgentBundle:Common:select-agent-team.html.twig' with {
				'name': "newticket[agent_team_id]",
				'id': baseId ~ '_agent_team',
				'agent_teams': ticket_options.agent_teams,
				'with_blank': true,
				'with_my_teams': true,
				'with_my_team_default': true
			} %}
		</div>
		<br class="clear" />
	</div>
</div>

<div class="section subject-section" id="{{baseId}}_subject_section">
	<div class="step">
		<span class="num">3</span>
		<span class="check">Done</span>
	</div>
	<div class="content">
		<h4>Subject</h4>
		<input type="text" name="newticket[subject]" id="{{baseId}}_subject" value="" />
	</div>
</div>

<div class="section message-section" id="{{baseId}}_message_section">
	<div class="step">
		<span class="num">4</span>
		<span class="check">Done</span>
	</div>
	<div class="content">
		<h4>Message</h4>
		<div class="type-wrap">
			Template: <select name="ticket[notification_template]">
				<option value="">Ticket</option>
				<option value="blank">Blank</option>
			</select>
		</div>
		<textarea name="newticket[message]" class="ticket-message" id="{{baseId}}_message"></textarea>
	</div>
</div>

<div class="section other-section" id="{{baseId}}_other_section">
	<div class="step">
		<span class="num">5</span>
		<span class="check">Done</span>
	</div>
	<div class="content">
		<h4>Other Properties</h4>

		{# Tab Container #}
		<div class="tab-container">
			<nav>
				<ul id="{{baseId}}_other_props_tabs">
					<li data-tab-for="> .tab-properties">Properties</li>
					<li data-tab-for="> .tab-cc">CC's</li>
					<li data-tab-for="> .tab-attach">Attachments</li>
				</ul>
			</nav>
			<div class="content-wrapper" id="{{baseId}}_other_props_tabs_content">
				{# Tab Properties #}
				<div class="tab-properties" style="display: none">
					<dl>
						<dt class="prop">Category</dt>
						<dd class="prop">
							{% include 'AgentBundle:Common:select-category.html.twig' with {
								'name': "newticket[category_id]",
								'id': baseId ~ '_cat',
								'categories': ticket_options.ticket_categories_hierarchy,
								'with_blank': true
							} %}
						</dd>

						<dt class="prop">Priority</dt>
						<dd class="prop">
							{% include 'AgentBundle:Common:select-priority.html.twig' with {
								'name': "newticket[priority_id]",
								'id': baseId ~ '_pri',
								'priorities': ticket_options.ticket_priorities,
								'with_blank': true
							} %}
						</dd>

						<dt class="prop">Workflow</dt>
						<dd class="prop">
							{% include 'AgentBundle:Common:select-workflow.html.twig' with {
								'name': "newticket[workflow_id]",
								'id': baseId ~ '_work',
								'workflows': ticket_options.ticket_workflows,
								'with_blank': true
							} %}
						</dd>

						<dt class="prop">Product</dt>
						<dd class="prop">
							{% include 'AgentBundle:Common:select-product.html.twig' with {
								'name': "newticket[product_id]",
								'id': baseId ~ '_work',
								'products': ticket_options.products_hierarchy,
								'with_blank': true
							} %}
						</dd>
					</dl>
				</div>
				{# /Tab Properties #}

				{# Tab CC #}
				<div class="tab-cc" style="display: none">
					<div class="cc-input">
						<input type="text" class="new-cc" id="{{baseId}}_add_cc_txt" /> <button class="clean-gray xxx-small add-cc-trigger">Add</button>
					</div>
					<ul class="cc-list" id="{{baseId}}_cc_list"></ul>
				</div>
				{# /Tab CC #}

				{# Tab Attach #}
				<div class="tab-attach" style="display: none">
					<script class="template-upload" type="text/x-jquery-tmpl">
						<li class="uploading">${name} <span class="cancel"><button class="clean-gray xx-small">Cancel</button></span></li>
					</script>
					<script class="template-download" type="text/x-jquery-tmpl">
						<li class="uploaded">
							<input type="checkbox" checked="checked" name="newticket[attach][]" value="${blob_id}" />
							<a href="${download_url}" target="_blank">${filename}</a>
							<span class="size">${filesize_readable}</span>
						</li>
					</script>
					<h6>Attachments</h6>
					<div class="file-input">
						<input type="file" name="file-upload" multiple="multiple" /> <button class="clean-gray xxx-small">Upload</button>
					</div>
					<ul class="files file-list"></ul>
				</div>
				{# /Tab Attach #}
			</div>
		</div>
		{# /Tab Container #}
	</div>
</div>

<div class="foot">
	<button class="dp-button submit-trigger">Create Ticket</button>
</div>

</form>

{# scroll containers #}
</div></div>
</div>
{# /scroll containers #}