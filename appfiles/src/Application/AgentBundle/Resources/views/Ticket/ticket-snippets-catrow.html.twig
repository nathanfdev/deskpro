<li class="cat-title-{{category.id}}" data-category="{{ category.id }}" data-tab-for="article.cat-{{ category.id }}">
	{#<span class="count">{{ ticket_snippets[category.id]|length|default(0) }}</span>#}
	<a>
		<span class="edit-cat-trigger"></span>
		{{ category.title }}
	</a>
</li>
