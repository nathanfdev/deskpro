{% for part in ticket.participants %}
	{% if part.person.is_agent %}
		<li class="person-{{part.person.id}}">
			<img src="{{ part.person.getPictureUrl(15) }}" width="15" height="15" /> {{ part.person.getDisplayContactShort(5) }}
		<li>
	{% endif %}
{% endfor %}