{% extends 'AgentBundle:Common:profile-contact-overlay.html.twig' %}
{% block list_extra %}
	{% for email in person.emails %}
		<li class="email" data-remove-name="remove_emails[]" data-remove-value="{{ email.id }}">
			<div class="type">Email</div>
			<div class="input">
				<div class="label">
					<input type="text" name="emails_comment[{{ email.id }}]" value="{{ email.comment }}" placeholder="Label" />
				</div>
				<div class="field">
					<input type="text" name="emails[{{ email.id }}]" value="{{ email.email }}" />
				</div>
			</div>
			<div class="remove">-</div>
		</li>
	{% endfor %}
{% endblock %}
{% block newmenu_options_extra %}
	<li data-tpl="tpl-new-email">Email</li>
{% endblock %}
{% block newtpl_extra %}
	<script type="text/x-deskpro-plain" class="tpl-new-email">
		<li class="email new">
			<div class="type">Email</div>
			<div class="input">
				<div class="label">
					<input type="text" name="new_emails_comment[]" value="" placeholder="Label" />
				</div>
				<div class="field">
					<input type="text" name="new_emails[]" value="" placeholder="Email address" />
				</div>
			</div>
			<div class="remove">-</div>
		</li>
	</script>
{% endblock %}
