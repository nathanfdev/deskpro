<div class="blob-row blob-{{blob.id}}" data-blob-id="{{ blob.id }}">
	<div class="title">
		<span>{{ blob.filename }}</span>
		<label>
			Title:
			<input type="text" class="title" value="{{ blob.title|default(blob.filename) }}" />
		</label>
		<div class="tags">
			<span class="tag-icon"></span>
			<ul>
				{% for blob in blob.labels %}
					<li><span>{{label.label}}</span></li>
				{% endfor %}
			</ul>
		</div>
	</div>
	<div class="info">
		<ul>
			<li><a href="{{ blob.download_url }}">Download file</a></li>
			<li>File zie: {{ blob.readable_filesize }}</li>
			{% if blob.dim_w %}
				<li>Dimentions: {{ blob.dim_w }}x{{ blob.dim_h }}</li>
			{% endif %}
		</ul>
	</div>
</div>