{% extends 'AdminBundle::layout.html.twig' %}
{% block pagetitle %}
	<h1>Setup Outgoing Email</h1>
{% endblock %}
{% block page %}

<form action="{{ path('admin_emailtrans_setup') }}" method="post">
{{ form_token('edit_transport') }}

<input type="hidden" name="process" value="1" />

<div class="dp-form">
	<input type="hidden" name="transport[match_type]" value="all" />

	<div class="dp-form-row">
		<div class="dp-form-label">
			<label>Default "From" email address</label>
		</div>
		<div class="dp-form-input">
			<input type="text" name="default_from_email" value="{{ app.getSetting('core.default_from_email') }}" />
		</div>
	</div>

	<div class="dp-form-row-group" data-element-handler="DeskPRO.Admin.ElementHandler.RadioExpander" data-group-class="dp-input-group" data-expand-class="dp-group-options">
		<div class="dp-form-row">
			<div class="dp-form-label">
				<label>How to send email</label>
			</div>
			<div class="dp-form-input">
				<div class="dp-input-group">
					<label>
						<input type="radio" class="option-trigger" name="transport[transport_type]" value="mail" {% if edittrans.transport_type == 'mail' %}checked="checked"{% endif %} />
						PHP mail()
					</label>
				</div>

				<div class="dp-input-group">
					<label>
						<input type="radio" class="option-trigger" name="transport[transport_type]" value="smtp" {% if not edittrans.transport_type or edittrans.transport_type == 'smtp' %}checked="checked"{% endif %} />
						SMTP Account
					</label>
					<div class="dp-group-options" style="display: none">
						<table cellspacing="0" cellpadding="0" width="100%">
							<tr>
								<td width="80">Host:</td>
								<td>
									{{ form_widget(form.smtp_options.host, {'attr': {'class': 'small'}}) }}<br />
								</td>
							</tr>
							<tr>
								<td>Secure:</td>
								<td>{{ form_widget(form.smtp_options.secure, {'attr': {'class': 'small'}}) }}</td>
							</tr>
							<tr>
								<td>Port:</td>
								<td>{{ form_widget(form.smtp_options.port, {'attr': {'class': 'small', 'style': 'width: 100px'}}) }}</td>
							</tr>
							<tr>
								<td>Username:</td>
								<td>{{ form_widget(form.smtp_options.username, {'attr': {'class': 'small'}}) }}</td>
							</tr>
							<tr>
								<td>Password:</td>
								<td>{{ form_widget(form.smtp_options.password, {'attr': {'class': 'small'}}) }}</td>
							</tr>
						</table>
					</div>
				</div>
			</div>
		</div>
	</div>
</div>

<button class="clean-white">Save</button>
</form>

{% endblock %}
