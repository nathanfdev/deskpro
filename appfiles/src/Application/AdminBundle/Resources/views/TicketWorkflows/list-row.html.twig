{% import 'AdminBundle:Common:design-macros.html.twig' as design %}
{% if not depth %}
<tbody>
{% endif %}
	<tr data-item-id="{{ workflow.id }}" class="depth-{{ depth|default(0) }} product product-{{workflow.id}}" data-product-id="{{workflow.id}}">
		<td class="title">
			<a href="{{ path('admin_ticketworks_edit', {'workflow_id': workflow.id}) }}" class="popout-trigger" data-height="200" data-width="400">
				{{workflow.title}}
			</a>
		</td>
	</tr>
{% if not depth %}
</tbody>
{% endif %}
