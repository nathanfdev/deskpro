{% extends 'AdminBundle::layout.twig.html' %}
{% block content %}

<div class="block">
	<div class="content">
		<h2 class="title">{% if style.id %}Edit Style{% else %}Create Style{% endif %}</h2>
		<div class="inner">
			<form action="{{ path('admin_styles_editstyle', {'style_id': style.id|default(0)}) }}" method="post" class="form">
				<input type="hidden" name="process" value="1" />
				<div class="group">
					<label class="label">Title</label>
					{{form.get('title').render(['class': 'text_field'])|safe}}
				</div>
				{% if form.has('parent_id') %}
					<div class="group">
						<label class="label">Parent</label>
						{{form.get('parent_id').render()|safe}}
						<span class="description">Choose a parent you want to inherit all templates from</span>
					</div>
				{% endif %}
				<div class="group">
					<label class="label">Note</label>
					{{form.get('note').render(['rows': 10, 'cols': 50, 'class': 'text_area'])|safe}}
					<span class="description">Enter any notes about this style. Notes are only displayed to administrators.</span>
				</div>
				<div class="group navform wat-cf">
					<button class="button" type="submit">
						<img src="{% asset 'images/icons/tick.png' %}" alt="Save" /> Save
					</button>
					{% if style.id %}
						<a href="{{ path('admin_styles') }}" class="button">
							<img src="{% asset 'images/icons/cross.png' %}" alt="Cancel"/> Cancel
						</a>
					{% endif %}
				</div>
			</form>
		</div>
	</div>
</div>

{% endblock %}
{% block sidebar %}
{% endblock %}