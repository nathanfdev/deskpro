{% extends 'AdminBundle::layout.html.twig' %}
{% block pagetitle %}
<h1>{% if team.id %}Edit Team{% else %}New Team{% endif %}</h1>
	<nav>
		<a class="clean-white" href="{{ path('admin_agents') }}">&larr; Back to Agents</a>
	</nav>
{% endblock %}
{% block page %}

<form action="{{ path('admin_agents_teams_edit', {'team_id': team.id|default(0)}) }}" method="post">
<input type="hidden" name="process" value="1" />

<div class="dp-form">
	<div class="dp-form-row">
		<div class="dp-form-label">
			<label>Title</label>
		</div>
		<div class="dp-form-input">
			<input type="text" name="team[name]" value="{{team.name}}" />
		</div>
	</div>
	<div class="dp-form-row">
		<div class="dp-form-label">
			<label>Members</label>
		</div>
		<div class="dp-form-input">
			<div class="checkbox-rows">
				<ul>
					{% for agent in agents %}
					<li>
						<label>
							<input type="checkbox" name="team[members][]" value="{{ agent.id }}" {% if team.members[agent.id] %}checked="checked"{% endif %} />
							{{ agent.display_name }}
						</label>
					</li>
					{% endfor %}
				</ul>
			</div>
		</div>
	</div>
</div>

<button class="btn primary save-trigger">Save</button>
</form>

<div class="delete-link-wrap">
	<a href="{{ path('admin_agents_teams_del', {'team_id': team.id, 'security_token': security_token('delete_team')}) }}" onclick="return confirm('Are you sure you want to delete this team?');">delete team</a>
</div>

{% endblock %}
