<li class="gateway gateway-{{gateway.id}}" data-gateway-id="{{gateway.id}}">
	<div style="float:right">
		<ul class="controls">
			<li class="edit edit-trigger"><a href="{{ path('admin_emailgateways_edit', {'gateway_id': gateway.id}) }}" class="popout-trigger" data-height="350" data-width="400">edit</a></li>
		</ul>
	</div>
	<div>
		{{gateway.title}} &lt;{{gateway.address}}&gt;
	</div>
</li>