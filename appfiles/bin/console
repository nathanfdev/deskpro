#!/usr/bin/env php
<?php

define('DP_ROOT', realpath(__DIR__ . '/../'));
require_once __DIR__.'/DeskproKernel.php';

use Symfony\Bundle\FrameworkBundle\Console\Application;

$kernel = new DeskproKernel('prod', true);

$application = new Application($kernel);
$application->run();
