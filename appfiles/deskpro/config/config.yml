parameters:
    kernel.include_core_classes: false

kernel.config:
  error_handler_level: null

web.config:
  router: { resource: "%kernel.root_dir%/config/routing.yml" }

web.templating:
  escaping: htmlspecialchars

twig.loader.class: "DeskPRO\\Twig\\Loader\\Hyrbid"
twig.loader.argument: [service_container]

doctrine.orm: ~
doctrine.dbal:
  driver: PDOMySql
  dbname: dp_400
  user: root
  password: mysquirrel