imports:
    - { resource: config.yml }

web.config:
    toolbar: true

twig.options:
  auto_reload: true

zend.logger:
    priority: debug
    path:     %kernel.root_dir%/logs/%kernel.environment%.log
