{
  "description": "schema of a search query",
  "type": "array",
  "items": {
    "type": "object",
    "oneOf": [
      { "$ref": "#/definitions/op" },
      { "$ref": "#/definitions/exactValue" },
      { "$ref": "#/definitions/dateTime" },
      { "$ref": "#/definitions/numericRange" }
    ],
    "required": ["type", "value"]
  },
  "definitions": {
    "op": {
      "properties": {
        "type": {
          "enum": ["OP_AND", "OP_OR", "OP_NOT"]
        },
        "meta": {
          "type": "object",
          "properties": {
            "isExplicit": {
              "type": "boolean"
            }
          },
        },
        "value": {
          "type": "array",
          "items": {
            "type": "object",
            "oneOf": [
              { "$ref": "#/definitions/op" },
              { "$ref": "#/definitions/exactValue" },
              { "$ref": "#/definitions/dateTime" },
              { "$ref": "#/definitions/numericRange" }
            ],
            "required": ["type", "value"]
          }
        }
      },
      "additionalProperties": false
    },
    "exactValue": {
      "properties": {
        "type": {
          "type": "string"
        },
        "value": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "number"
            }
          ]
        }
      },
      "additionalProperties": false
    },
    "dateTime": {
      "properties": {
        "type": {
          "type": "string"
        },
        "value": {
          "type": "object",
          "oneOf": [
            { "$ref": "#/definitions/dateTime/definitions/dateTimePreset" },
            { "$ref": "#/definitions/dateTime/definitions/dateTimeAbsolute" }
          ],
          "required": ["inputType"]
        }
      },
      "additionalProperties": false,
      "definitions": {
        "dateTimePreset": {
          "properties": {
            "inputType": {
              "enum": ["preset"]
            },
            "preset": {
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        "dateTimeAbsolute": {
          "properties": {
            "inputType": {
              "enum": ["absolute"]
            },
            "date": {
              "type": "string",
              "format": "date-time"
            },
            "op": {
              "enum": ["=", ">", "<", "range"]
            }
          },
          "additionalProperties": false
        }
      }
    },
    "numericRange": {
      "properties": {
        "type": {
          "type": "string"
        },
        "value": {
          "type": "array",
          "items": {
            "type": "number"
          }
        }
      },
      "additionalProperties": false
    }
  }
}