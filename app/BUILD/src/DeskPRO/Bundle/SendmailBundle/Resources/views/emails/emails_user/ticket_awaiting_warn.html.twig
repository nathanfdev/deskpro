<dp:subject>{{ phrase('user.email_subjects.remind_unresolved_subject', {subject: ticket.subject}) }}</dp:subject>

<container>
    {{ phrase('user.emails.greeting') }}

    <br /><br />

    {{ phrase('user.emails.remind_unresolved', {helpdesk_name: brand_setting('core.deskpro_name'), resolve_url: portal_url(ticket, 'resolve')})|raw }}
</container>
