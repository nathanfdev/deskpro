<dp:subject>{{ phrase('portal.email_subjects.ticket_added_cc') }}</dp:subject>

<container>
    {{ phrase('portal.emails.greeting') }}

    <br /><br />

    {{ phrase('portal.emails.ticket_cc-added') }}

    {% if app.isPortalEnabled() %}
        <br /><br />

        {{ phrase('portal.emails.ticket_access_ticket_online') }}
        <a href="{{ portal_url(ticket) }}">{{ portal_url(ticket) }}</a>
    {% endif %}

    <br /><br />

    {{ phrase('portal.emails.end_greeting')| raw }}
</container>
