<dp:subject>{{ phrase('portal.email_subjects.comment-confirmation') }}</dp:subject>
<container>
    {% set phrase_link %}<a href="{{ content_link }}">{{ content.title }}</a>{% endset %}
    {{ phrase('portal.emails.comment_thank-you', {'link':phrase_link}, true) }}
</container>
