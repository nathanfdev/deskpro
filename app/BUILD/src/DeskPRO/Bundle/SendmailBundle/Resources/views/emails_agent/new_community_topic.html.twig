{{ set_tplvar('agent_notification_footer', 'notify') }}
<dp:subject>
	{{ phrase('agent.community.new_community') }}	{{ topic.title }} {% if topic.status == 'hidden' and not topic.is_reviewed %}({{ phrase('agent.general.requires_validation') }}){% endif %}
</dp:subject>

<html>
<head>
	{% include 'SendmailBundle:blocks:resources.html.twig' %}
</head>
<body>
{% include 'SendmailBundle:emails_common:email_code_top.html.twig' %}

{% include 'SendmailBundle:blocks:header.html.twig' %}

	{{ phrase('agent.community.user_submitted_new_community_topic', {name: person.display_contact }) }}
	{% if topic.status == 'hidden' and not topic.is_reviewed %}({{ phrase('agent.general.requires_validation') }}){% endif %}

	<br /><br />

	{{ phrase('agent.emails.view_online_at') }} <a href="{{ login_link }}#app.community,i:{{ topic.id }}">{{ login_link }}#app.community,ct_content,i:{{ topic.id }}</a>

	<br /><br />

	{{ topic.content|raw }}

{% include 'SendmailBundle:blocks:footer.html.twig' %}

{% include 'SendmailBundle:emails_common:email_code_bottom.html.twig' %}
</body>
</html>
