<dp:subject>{{ phrase('helpcenter.emails.too_many_emails') }}</dp:subject>

<html>
<head>
	{% include 'SendmailBundle:blocks:resources.html.twig' %}
</head>
<body>
{% include 'SendmailBundle:emails_common:email_code_top.html.twig' %}

{% include 'SendmailBundle:blocks:header.html.twig' %}

    {{ phrase('helpcenter.emails.ratelimit_explain',{num_messages: num_messages, time_limit: time_limit, time_lock: time_lock, date_lock_end: date_lock_end}) }}

    {% if app.isPortalEnabled() and can_login(recipient.id) %}
        <br /><br />

        {{ phrase('helpcenter.emails.ratelimit_submit_online') }}
        <a href="{{ deskpro_url }}new-ticket">{{ deskpro_url }}new-ticket</a>
    {% endif %}

{% include 'SendmailBundle:blocks:footer.html.twig' %}

{% include 'SendmailBundle:emails_common:email_code_bottom.html.twig' %}
</body>
</html>
