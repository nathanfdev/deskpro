<dp:subject>{{ phrase('portal.email_subjects.email_new-confirm') }}</dp:subject>

<html>
<head>
	{% include 'SendmailBundle:blocks:resources.html.twig' %}
</head>
<body>
{% include 'SendmailBundle:blocks:header.html.twig' %}

    <container>
        {{ phrase('portal.emails.ticket_validate') }}

        <br /><br />

        {{ phrase('portal.emails.profile_email_link-validate') }}:
        {% if verify_url %}<a href="{{ verify_url }}">{{ verify_url }}</a>{% else %}<a href="{{ url_full('user_validate_ticket', {'access_code': access_code}) }}">{{ url_full('user_validate_ticket', {'access_code': access_code}) }}</a>{% endif %}
    </container>

  {% include 'SendmailBundle:blocks:footer.html.twig' %}
</body>
</html>
