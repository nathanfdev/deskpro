<dp:subject>{{ phrase('portal.email_subjects.email_primary-confirm') }}</dp:subject>

<container>
    {{ phrase('portal.emails.profile_email-primary-confirm') }}:<br/>
    <a href="{{ verify_url }}">{{ verify_url }}</a>
</container>
