<dp:subject>{{ phrase('portal.email_subjects.ticket_new-participant') }}</dp:subject>

<container>
	{% if is_first %}
		{{ phrase('portal.emails.ticket_cc-new', {'name':ticket.person.display_name_user}) }}
		<br /><br />
	{% endif %}

	{% for message in messages %}
		{{ message.getMessageHtml(false)|raw }}
	{% endfor %}
</container>
