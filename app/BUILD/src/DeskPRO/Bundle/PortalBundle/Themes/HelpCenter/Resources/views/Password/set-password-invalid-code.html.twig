{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
<div class="dp-po">
    <main class="dp-po-main">
        <div class="container">
            <div class="row">
                <div class="col-sm-7 col-centered">
                    <div class="dp-po-block">
                        <p>{{ phrase('helpcenter.account.set_password_invalid_desc') }}</p>

                        <p><a href="{{ path('portal_set_password') }}">{{ phrase('helpcenter.account.set_password_click_here') }}</a></p>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>
{% endblock page_content %}
