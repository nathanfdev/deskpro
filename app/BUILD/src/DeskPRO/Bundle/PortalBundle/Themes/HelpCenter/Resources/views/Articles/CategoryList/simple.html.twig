{% if category_children|length > 0 %}
    <div class="dp-po-kb-featured mb-0">
        <h2 class="dp-po-kb-featured-title">{{ phrase('helpcenter.general.categories') }}</h2>
        <div class="row">
            {% for category in category_children %}
                {% if loop.index0 > 0 and loop.index0 % 2 == 0 %}</div><div class="row">{% endif %}
                <div class="col-sm-6">
                    <div class="dp-po-category-title">
                        <a href="{{ portal_path(category) }}" class="dp-po-category-title-link">
                            <span class="dp-po-category-title-icon" role="presentation">
                                {{ render_icon_from(category) }}
                            </span>
                            <h2 class="dp-po-category-title-text">{{ object_phrase(category) }} <span>({{ category_children_pagers[category.id]|length }})</span></h2>
                        </a>
                        <a href="javascript:void(0)" class="dp-po-category-title-expand">
                            <i class="dp-po-icon fal fa-angle-down"></i>
                        </a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endif %}
