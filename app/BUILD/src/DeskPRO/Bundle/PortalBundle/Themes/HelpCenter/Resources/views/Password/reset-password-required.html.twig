{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    <header class="section-header">
        <h1>{{ phrase('helpcenter.account.set-password-sent-title') }}</h1>
    </header>

    <p>{{ phrase('helpcenter.account.set-password-expired') }}</p>
    <p>{{ phrase('helpcenter.account.set-password-sent-desc') }}</p>

{% endblock page_content %}
