{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    <div class="dp-po">
        <main id="main" tabindex="-1" class="dp-po-main">
            <header class="section-header">
                <h1>{{ phrase('helpcenter.account.set_password_sent_title') }}</h1>
            </header>

            <p>{{ phrase('helpcenter.account.set_password_expired') }}</p>
            <p>{{ phrase('helpcenter.account.set_password_sent_desc') }}</p>
        </main>
    </div>

{% endblock page_content %}
