{% extends 'Theme::layout.html.twig' %}

{% block page_body %}
    <div class="dp-po-title">
        <h2>{{ phrase('helpcenter.account.section_title_usersource_email') }}</h2>
    </div>

    <div class="dp-po-block">
       <p>{{ phrase('helpcenter.account.usersource_set_email_desc') }}</p>

        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">{{ phrase('helpcenter.account.email_btn_usersource_set') }}</button>
        {{ form_end(form) }}
    </div>
{% endblock page_body %}
