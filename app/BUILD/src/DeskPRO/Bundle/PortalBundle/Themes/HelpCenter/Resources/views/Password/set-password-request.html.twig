{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    {% if auth_manager.isForgotPasswordVisible %}
        <header class="section-header">
            <h1>{{ phrase('helpcenter.account.set-password-request-title') }}</h1>
        </header>

       {{ phrase('helpcenter.account.set-password-request-desc') }}

        {% if render_error %}
            <strong>{{ phrase('helpcenter.account.set-password-invalid-input') }}</strong>
        {% endif %}

        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button type="submit">{{ phrase('helpcenter.account.set-password-btn') }}</button>
        {{ form_end(form) }}
    {% endif %}
{% endblock page_content %}
