{% include 'Theme:Community:CommunityTopicsList/topics-#{topics_list.view}.html.twig' with {
    topics: topics_list.topics_data.pager,
    filtered: topics_list.topics_data.filtered,
    is_compact: topics_list.is_compact,
    activities_per_topic: topics_list.topics_data.activities_per_topic,
    view: view,
} %}

{% include 'Theme:Common:pager.html.twig' with { pager: topics_list.topics_data.pager } %}
