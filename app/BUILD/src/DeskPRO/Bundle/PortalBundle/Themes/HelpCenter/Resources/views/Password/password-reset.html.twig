{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    {% if auth_manager.isForgotPasswordVisible %}
        <div class="dp-po">
            <main id="main" tabindex="-1" class="dp-po-main">
                <div class="container">
                    <div class="row">
                        <div class="col-sm-7 col-centered">
                            <div class="dp-po-title">
                                <h2>{{ phrase('helpcenter.account.section_title_reset_password') }}</h2>
                            </div>

                            <div class="dp-po-block">

                                <div class="form-ticket">
                                    {{ form_start(form) }}
                                    {{ form_rest(form) }}
                                    <div class="column-full">
                                        <div class="bucket">
                                            <button class="btn btn-primary" type="submit">{{ phrase('helpcenter.account.reset_password_action') }}</button>
                                        </div>
                                    </div>
                                    {{ form_end(form) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </main>
        </div>
    {% endif %}
{% endblock page_content %}
