{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    <p>{{ phrase('helpcenter.account.reset-password-invalid-desc') }}</p>

    <p><a href="{{ path('portal_reset_password') }}">{{ phrase('helpcenter.account.reset-password-click-here') }}</a></p>
{% endblock page_content %}
