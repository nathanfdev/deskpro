{% extends 'Theme::layout.html.twig' %}
{% import 'Theme:Portal:User/macros.html.twig' as macros %}

{% block page_content %}
    <div class="dp-po">
        <main id="main" tabindex="-1" class="dp-po-main dp-po-ticket-create">
            <div class="container">
                <div class="row">
                    <div class="col-sm-7 col-centered">
                        {% if no_login %}
                            {{  macros.user_notice('auth', phrase('helpcenter.account.new_ticket_no_login')) }}
                        {% else %}
                            {{  macros.lockout_notice(phrase('helpcenter.account.tickets_lockout'), lockout, lockout_time) }}
                        {% endif %}
                        <div class="dp-po-title">
                            <img class="dp-po-title-svg" src="{{ asset('img/page-icons/contact-us.svg', 'help_center') }}">
                            <h1 class="dp-po-title-text">{{ phrase('helpcenter.tickets.new_section_title') }}</h1>
                        </div>
                        <div class="dp-po-block">
                            {% if rerendering %}
                                <strong>{{ phrase('helpcenter.tickets.form_changed_notice') }}</strong>
                            {% endif %}

                            {% if rerendering_saved %}
                                <strong>{{ phrase('helpcenter.general.rerender_form') }}</strong>
                            {% endif %}
                            {% include 'Theme:NewTicket:new_ticket_form.html.twig' %}
                        </div>
                    </div>
                </div>
            </div>
        </main>
    </div>
{% endblock page_content %}
{% block doc_end %}
    <!-- DESKPRO_TICKET_DISPLAY -->
    <script type="text/javascript">
        {{ ticket_display_js|raw }}
    </script>
{% endblock doc_end %}
