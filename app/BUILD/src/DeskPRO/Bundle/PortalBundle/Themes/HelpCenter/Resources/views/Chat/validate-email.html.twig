{% extends 'Theme::layout.html.twig' %}

{% block page_body %}
    {% if is_valid %}
        {{ phrase('helpcenter.messenger.email_validated_sucessfully') }}
    {% else %}
        {{ phrase('helpcenter.messenger.invalid_validation_code') }}
    {% endif %}
{% endblock page_body %}
