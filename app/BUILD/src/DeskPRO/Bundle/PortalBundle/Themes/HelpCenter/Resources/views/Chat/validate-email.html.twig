{% extends 'Theme::layout.html.twig' %}

{% block page_body %}
    {% if is_valid %}
        {{ phrase('helpcenter.chat.email_validated_sucessfully') }}
    {% else %}
        {{ phrase('helpcenter.chat.invalid_validation_code') }}
    {% endif %}
{% endblock page_body %}
