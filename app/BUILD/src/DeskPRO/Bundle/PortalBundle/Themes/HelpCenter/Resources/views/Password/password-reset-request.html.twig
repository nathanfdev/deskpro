{% extends 'Theme::layout.html.twig' %}
{% import 'Theme:Portal:User/macros.html.twig' as macros %}

{% block page_content %}
    {{  macros.lockout_notice(phrase('helpcenter.account.reset-password-lockout'), lockout, lockout_time) }}
    {% if auth_manager.isForgotPasswordVisible %}
        <header class="section-header">
            <h1>{{ phrase('helpcenter.account.reset-password-request-title') }}</h1>
        </header>

       {{ phrase('helpcenter.account.reset-password-request-desc') }}

        {% if render_error %}
            <strong>{{ phrase('helpcenter.account.reset-password-invalid-input') }}</strong>
        {% endif %}

        <div class="form-ticket">
            {{ form_start(form) }}
            {{ form_rest(form) }}
            <div class="column-full">
                <div class="bucket">
                    <button type="submit">{{ phrase('helpcenter.account.reset-password-btn') }}</button>
                </div>
            </div>
            {{ form_end(form) }}
        </div>
    {% endif %}
{% endblock page_content %}
