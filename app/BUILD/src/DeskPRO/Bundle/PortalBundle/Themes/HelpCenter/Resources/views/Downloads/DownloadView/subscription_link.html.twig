{% if brand_setting('user.downloads_subscriptions') %}
    <a href="{{ portal_path(file, 'toggle_subscription') }}" class="post-link" rel="noindex, nofollow">
        {% if is_subscribed %}
            {{ phrase('helpcenter.general.btn-unsubscribe') }}
        {% else %}
            {{ phrase('helpcenter.general.btn-subscribe') }}
        {% endif %}
        <i class="fas fa-rss"></i>
    </a>
{% endif %}
