<div class="dp-po-profile-activity-icon">
    <i class="dp-po-icon far fa-chevron-circle-down"></i>
</div>
<div class="dp-po-profile-activity-desc">
    <a href="{{ path('portal_downloads_view', {slug: activity.slug}) }}" class="dp-po-profile-activity-link">
        {{ phrase('portal.members.activity.down_voted') }}
        <img class="dp-po-profile-activity-image" src="{{ asset('img/page-icons/download.svg', 'help_center') }}">
        <span>{{ activity.description|truncate(truncateDescription) }}</span>
    </a>
</div>
