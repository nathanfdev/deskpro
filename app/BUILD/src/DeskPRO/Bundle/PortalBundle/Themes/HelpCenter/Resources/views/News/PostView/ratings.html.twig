{% if can_rate_news(post) %}
<article class="news kb-article">
    <div class="rate-article">
        <div class="titled-header">
            <h1>{{ phrase('helpcenter.general.rate_page') }}</h1>
        </div>

        {% if rating %}
            <div class="rate-article {% if rating.positive %}as-positive{% else %}as-negative{% endif %}">
                {% if rating.positive %}
                    {{ phrase('helpcenter.general.rated_positive') }}
                {% else %}
                    {{ phrase('helpcenter.general.rated_negative') }}
                {% endif %}
            </div>
        {% endif %}

        <div class="rate-article">
            <a href="{{ portal_path(post, 'vote_up') }}" class="rate-up post-link" rel="noindex, nofollow">
                {{ phrase('helpcenter.general.rate_page_positive') }} <span>:)</span>
            </a>
            <a href="{{ portal_path(post, 'vote_down') }}" class="rate-down post-link" rel="noindex, nofollow">
                <span>:(</span> {{ phrase('helpcenter.general.rate_page_negative') }}
            </a>
        </div>

        {% if show_rating_counts and rating_counts['total'] > 0 %}
            <p>{{ phrase('helpcenter.general.rating_x_of_y_found_helpful', { vote_up: rating_counts['positive'], vote_count: rating_counts['total'] }) }}</p>
        {% endif %}
    </div>
</article>
{% endif %}
