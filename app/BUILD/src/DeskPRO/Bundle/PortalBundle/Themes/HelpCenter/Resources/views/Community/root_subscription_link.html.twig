{% if brand_setting('user.community_subscriptions') %}
    <a href="{{ path('portal_community_root_toggle_subscription') }}" class="post-link" rel="noindex, nofollow">
      {% if is_subscribed %}
        {{ phrase('helpcenter.general.btn-unsubscribe') }}
      {% else %}
          {{ phrase('helpcenter.general.btn-subscribe') }}
      {% endif %}
    </a>
{% endif %}
