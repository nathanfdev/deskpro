{% extends 'Theme::layout.html.twig' %}

{% block page_body %}
    <div class="dp-po-title">
        <h2>{{ phrase('helpcenter.account.set-password-sent-title') }}</h2>
    </div>

    <div class="dp-po-block">
        <p>{{ phrase('helpcenter.account.registration-set-password') }}</p>

        <p>{{ phrase('helpcenter.account.set-password-sent-to') }}: <strong>{{ email }}</strong></p>
    </div>
{% endblock page_body %}
