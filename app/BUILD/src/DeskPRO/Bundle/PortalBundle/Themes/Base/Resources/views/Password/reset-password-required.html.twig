{% extends 'Theme::layout.html.twig' %}

{% block page_content %}
    <div class="breadcrumbs">
        {% show breadcrumbs %}
    </div>

    <header class="section-header">
        <h1>{{ phrase('portal.account.set-password-sent-title') }}</h1>
    </header>

    <p>{{ phrase('portal.account.set-password-expired') }}</p>
    <p>{{ phrase('portal.account.set-password-sent-desc') }}</p>

{% endblock page_content %}
