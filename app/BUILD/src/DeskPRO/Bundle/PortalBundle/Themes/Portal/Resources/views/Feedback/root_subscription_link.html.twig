{% if brand_setting('user.feedback_subscriptions') %}
    <a href="{{ path('portal_feedback_root_toggle_subscription') }}" class="post-link" rel="noindex, nofollow">
      {% if is_subscribed %}
        {{ phrase('portal.general.btn-unsubscribe') }}
      {% else %}
          {{ phrase('portal.general.btn-subscribe') }}
      {% endif %}
    </a>
{% endif %}
