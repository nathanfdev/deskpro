{% for provider in providers %}
    <a href="{{ path(type ~ '_login_authenticate', {'usersource_id': usersource.id, return: return, "social-login": provider.name}) }}"><img src="{{ asset("images/site-icons/" ~  provider.name ~ ".png" ) }}" /></a>
{% endfor %}



