<dp:subject>{{ phrase('portal.email_subjects.ticket_added_cc') }}</dp:subject>

{{ phrase('portal.emails.greeting') }}

<br /><br />

{{ phrase('portal.emails.ticket_cc-added', { author_name: action_performer.name, author_email: action_performer.email })| raw }}

<br /><br />

{% if app.isPortalEnabled() %}
    {{ phrase('portal.emails.ticket_cc-added-reply-view') }}
    <a href="{{ portal_url(ticket) }}">{{ portal_url(ticket) }}</a>
{% else %}
    {{ phrase('portal.emails.ticket_cc-added-reply') }}
{% endif %}

<br /><br />

<dp:ticket-messages />
