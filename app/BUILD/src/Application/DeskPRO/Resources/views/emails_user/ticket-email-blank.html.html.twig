<dp:subject>{{ phrase('user.email_subjects.tickets_re') }}</dp:subject>
{{ message|raw }}

{% if app.isPortalEnabled() %}
	<br /><br />

	{{ phrase('user.emails.ticket_access_ticket_online') }}
	<a href="{{ portal_url(ticket) }}">
		{{ portal_url(ticket) }}
	</a>
{% endif %}
