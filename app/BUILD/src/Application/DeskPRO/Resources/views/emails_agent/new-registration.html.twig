{{ set_tplvar('agent_notification_footer', 'notify') }}
<dp:subject>
	{{ phrase('agent.people.new_registration_name', {name: person.getDisplayContact()}) }}
</dp:subject>
<dp:body>
	{{ phrase('agent.people.new_registration_name', {name: person.getDisplayContact()}) }}
