<div class="dp-codeeditor-modal" id="new-email-template-editor-modal"}>
	<div>
		<div class="modal-header">
			<button type="button" class="close" ng-click="dismiss()">&times;</button>
			<h4 class="modal-title" ng-if="!is_new_email">{{ ng_var('display_title') }}</h4>
			<h4 class="modal-title" ng-if="is_new_email">Create new email template</h4>
		</div>
		<div class="modal-body" id="new-email-template-editor"}></div>
	</div>
</div>
