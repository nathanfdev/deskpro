{% if brand_setting('user.kb_subscriptions') %}
    <a href="{{ portal_path(article, 'toggle_subscription') }}" class="post-link">
        {% if is_subscribed %}
            {{ phrase('portal.general.btn-unsubscribe') }}
        {% else %}
            {{ phrase('portal.general.btn-subscribe') }}
        {% endif %}
        <i class="fa fa-rss"></i>
    </a>
{% endif %}
