{% for item in pager %}
<div class="feedback-item feedback-list-item">
  <div class="feedback-item-controls">
    <a href="{{ portal_path(item, 'vote_up') }}" class="i-agree post-link {% if user_up_voted(item) %} agreed{% endif %}">
      <i class="fa fa-thumbs-up"></i>
      <div>{{ phrase('portal.feedback.btn-vote') }}</div>
      <span class="counter">{{ item.total_rating }}</span>
    </a>
    {# fav/stars feature not supported yet #}
    {#<a href="#" class="star">#}
      {#<i class="fa fa-star-o"></i>#}
      {#<div>2</div>#}
      {#</a>#}
    </div>

    <div class="feedback-item-content">
      <span class="feedback-status">
        {% if item.status_category %}
        <a href="{{ portal_path(item.status_category) }}" data-id="{{ item.status_category.id }}">
          <i class="fa fa-eye"></i> {{ item.status_category }}
        </a>
        {% else %}
        {{ item.status }}
        {% endif %}
      </span>

      <span class="feedback-item-type">
        <a href="{{ portal_path(item.category) }}" class="feedback-category" data-id="{{ item.category.id }}">{{ item.category }}</a>
      </span>

      <h1>
        <a href="{{ portal_path(item) }}">{{ item.title }}</a>
      </h1>

      <div class="comment">
        <p>{{ item.content_html|raw|slice(0,350) ~ '...' }}</p>
      </div>

      <div class="response">
        <div class="response-meta">

          <div class="avatar" style="background-image: url(https://secure.gravatar.com/avatar/79cfcfdf770db09a40fca15bb845fef1?&amp;s=80);"></div>
          Response by: <span>Chris Padfield</span>
        </div>
        <div class="response-content">
          <p>These suggestions have been submitted by customers like you; we encourage you to discuss and vote lorel ipsum dolor. Lorel ipsum some more text to have a third line.</p>
        </div>
      </div>


      <div class="article-meta"><!-- TODO this is just copied html for now, need to move stuff from feedback-item-meta below (we don't need .feedback-item-meta any more) -->
        By
        <a href="#"><span style="background: url(https://secure.gravatar.com/avatar/79cfcfdf770db09a40fca15bb845fef1?&amp;s=80) no-repeat; background-size: cover" class="agent-avatar agent-avatar-mini"></span>{{ item.person.display_name }}</a>
         in 2015-10-27
        <hr>

        {% if item.num_comments > 0 %}
        <span><a href="{{ portal_path(item) }}#comments"><i class="fa fa-comment"></i> {{ phrase('portal.general.comments-count', {count: item.num_comments}) }}</a></span>
        <hr>
        {% endif %}

        {% if item.getCustomDataSelection() %}
        <span><i class="fa fa-inbox"></i> {{ item.getCustomDataSelection().title }}</span>
        <hr>
        {% endif %}

        {% if item.labels|length %}
        <span>
          <i class="fa fa-tags"></i>
          {% for l in item.labels %}
          <a href="{{ path('portal_search_labels', { label: l }) }}">{{ l }}</a>{% if not loop.last %}, {% endif %}
          {% endfor %}
        </span>
        <hr>
        {% endif %}

        In <a href="/news/general">General</a>
      </div>

    </div>
    <!-- End feedback-item-content -->
  </div>
  <!-- End feedback-item -->
  {% else %}
  <strong>{{ phrase('portal.feedback.none') }}</strong>
  {% endfor %}
