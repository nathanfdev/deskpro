{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ this_download_breadcrumbs({category: file.category})|raw }}
    {{ this_download()|raw }}
    {{ this_downloads_ratings()|raw }}
    {{ this_downloads_comments()|raw }}
    {% include 'Theme:Common:comments_new_form.html.twig' with { new_comment_form: new_comment_form } %}
    {{ this_related_content()|raw }}
{% endblock inner_content %}
