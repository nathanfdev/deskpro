{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    <div class="breadcrumbs">
    {% show breadcrumbs %}
    </div>
    <div class="downloads browse-page">
    <header class="section-header padded">
        <h1>{{ category.title }}</h1>
        {% show download_cat_subscription_link %}
    </header>
    {% show download_cats %}
    <div class="column-full">
        {% show download_list_detail %}
        {% show download_pager %}
    </div>
    </div>
{% endblock inner_content %}
