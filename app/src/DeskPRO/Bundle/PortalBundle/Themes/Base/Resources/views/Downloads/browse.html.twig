{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ download_breadcrumbs() }}
    <h1>{{ category.realTitle }}</h1>
    {{ download_category_subscription() }}
    {{ downloads_list() }}
    {{ downloads_files_items({ show_category_link: false }) }}
    {{ downloads_pager() }}
{% endblock inner_content %}
