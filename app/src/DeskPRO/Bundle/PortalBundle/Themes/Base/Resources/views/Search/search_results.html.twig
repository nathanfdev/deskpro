{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
<h1>Search</h1>
    {{ dump(query) }}
    {{ dump(results) }}
    {{ dump(sticky_results) }}
    {{ dump(pageinfo) }}
    {{ dump(num_results) }}
{% endblock inner_content %}
