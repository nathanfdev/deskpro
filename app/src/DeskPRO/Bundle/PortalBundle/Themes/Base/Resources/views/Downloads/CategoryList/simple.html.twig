{% if category_children|length > 0 %}
<ul>
    {% for category in category_children %}
        <li>
            <a href="{{ path('portal_downloads_browse', { slug: category.slug }) }}">
                {{ category.title }}
            </a>
        </li>
    {% endfor %}
</ul>
{% endif %}
