{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ news_breadcrumbs() }}
    {{ news_list() }}
    <h1>{{ category.realTitle }}</h1>
    {{ news_category_subscription() }}
    {{ news_posts_pretty({ show_category_link: false }) }}
    {{ news_pager() }}
{% endblock inner_content %}
