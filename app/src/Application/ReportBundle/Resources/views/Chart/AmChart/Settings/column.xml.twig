<?xml version="1.0" encoding="UTF-8"?>
<settings>
	<type>column</type>
	<font>Tahoma</font>
	<redraw>1</redraw>
	<colors>{{ chart.getColorCodes|join(',') }}</colors>

	<strings>
		<no_data>{{ chart.getNoDataLabel }}</no_data>
	</strings>

	<column>
		<type></type>
		<width>85</width>
		<spacing>0</spacing>
		<grow_time>0</grow_time>
		<sequenced_grow>1</sequenced_grow>
	</column>

	<plot_area>
		<margins>
			<left>60</left>
			<top>20</top>
			<right>20</right>
			<bottom>60</bottom>
		</margins>
	</plot_area>

	<values>
		<category>
			<frequency>1</frequency>
			<start_from>0</start_from>
		</category>
	</values>

	<balloon>
		<text_color>FFFFFF</text_color>
		<text_size>16</text_size>
	</balloon>

	<error_messages>
		<color>#cccccc</color>
	</error_messages>

	{% if dashboard_stat.getShowLegend() == true %}
		<legend>
			<enabled>1</enabled>
		</legend>
	{% else %}
		<legend>
			<enabled>0</enabled>
		</legend>
	{% endif %}

	<axes>
		<x>
			<width>1</width>
		</x>
		<y_left>
			<width>1</width>
		</y_left>
	</axes>

	<labels>
		<label lid="0">
			<x>10</x>
			<y>100%</y>
			<rotate>true</rotate>
			<width></width>
			<align>center</align>
			<text><![CDATA[{{ chart.yLabel }}]]></text>
		</label>
		<label lid="1">
			<x>0</x>
			<y>!25</y>
			<align>center</align>
			<text><![CDATA[{{ chart.xLabel|default('xxx') }}]]></text>
		</label>
	</labels>

	<graphs>
		{% for label in chart.labels %}
			<graph gid="{{ loop.index }}">
				<type>column</type>
				<title>{{ label|default('') }}</title>
				<balloon_text>
					<![CDATA[{{ label|default('') }}: {value}{{ chart.getDisplayUnits }}]]>
				</balloon_text>
			</graph>
		{% endfor %}
	</graphs>

	<data>
		<chart>
			<series>
				{% for series in chart.series %}
					<value xid="{{ loop.index0 }}">{{ series }}</value>
				{% endfor %}
			</series>
			<graphs>
				{% for graphs in chart.graphs %}
					<graph gid="{{ loop.index }}">
						{% for point in graphs.data %}
							<value xid="{{ loop.index0 }}">{{ chart.formatData(point) }}</value>
						{% endfor %}
					</graph>
				{% endfor %}
			</graphs>
		</chart>
	</data>
</settings>
