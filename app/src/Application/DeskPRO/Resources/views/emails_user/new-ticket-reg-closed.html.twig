<dp:is-noreply />
<dp:subject>{{ phrase('user.email_subjects.re', {subject: subject}) }}</dp:subject>
<dp:body>
	<dp:section>
		{{ phrase('user.emails.registration_closed', {name: name}) }}
	</dp:section>
</dp:body>
