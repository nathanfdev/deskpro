<dp:subject>{{ phrase('user.email_subjects.ticket_new-participant') }}</dp:subject>
<dp:body>
	<dp:section>
		{% if is_first %}
			{{ phrase('user.emails.ticket_cc-new', {'name':ticket.person.display_name}) }}
			<br /><br />
		{% endif %}

		{% for message in messages %}
			{{ message.message_html|raw }}
		{% endfor %}
	</dp:section>
</dp:body>