{% set with_cutmark = 'DP_USER_EMAIL' %}
{% extends 'DeskPRO:emails_common:layout.html.twig' %}
{% block email_subject %}
	{{ phrase('user.email_subjects.ticket_new-participant') }}
{% endblock %}
{% block content %}
	<!--ACCESS CODE: (#{{access_code_full}})-->
	{% if is_first %}
		<p>
            {{ phrase('user.emails.ticket_cc-new', {'name':ticket.person.display_name}) }}
		</p>
	{% endif %}

	{% for message in messages %}
		{{ message.message_html|raw }}
	{% endfor %}
{% endblock %}