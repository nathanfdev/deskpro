<dp:subject>{{ phrase('user.email_subjects.too-many-emails') }}</dp:subject>

{{ phrase('user.emails.ratelimit_explain') }}

{% if app.isPortalEnabled() %}
    <br /><br />

    {{ phrase('user.emails.ratelimit_submit-online') }}
    <a href="{{ deskpro_url }}new-ticket">{{ deskpro_url }}new-ticket</a>
{% endif %}
