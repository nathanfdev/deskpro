{% extends 'DeskPRO:emails_user:layout.html.twig' %}
{% import 'DeskPRO:emails_user:layout-macros.html.twig' as layout %}
{% block email_teaser_link %}
	{{ phrase('user.emails_user.view_your_feedback_online_at') }}:<br />
	<a href="{{ url('user_feedback_view', {'slug': feedback.url_slug}) }}">
		{{ url('user_feedback_view', {'slug': feedback.url_slug}) }}
	</a>
{% endblock %}
{% block content %}
	{{ layout.section_start() }}
		{% if show == 'validated' %}
        {{ phrase('user.emails_user.your_feedback_had_been_validated_and_will_be_made_public', {'title':feedback.title}) }}
		{% elseif show == 'closed' %}
            {{ phrase('user.emails_user.your_feedback_was_closed', {'title':feedback.title, 'status':feedback.status_category.title}) }}
        {% elseif show == 'closed' %}
            {{ phrase('user.emails_user.your_feedback_has_been_updated', {'title': feedback.title, 'status':feedback.statuscategory.title}) }}
        {% endif %}
	{{ layout.section_end() }}

	{{ layout.section_start() }}
        {{ phrase('user.emails_user.your_feedback_has_count_votes', {'count': feedback.votes}) }}
		<a href="{{ url('user_feedback_view', {'slug': feedback.url_slug}) }}">
			{{ url('user_feedback_view', {'slug': feedback.url_slug}) }}
		</a>
	{{ layout.section_end() }}
{% endblock %}