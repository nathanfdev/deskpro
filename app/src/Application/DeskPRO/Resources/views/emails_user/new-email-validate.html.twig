<dp:is-noreply />
<dp:subject>{{ phrase('user.email_subjects.email_new-confirm') }}</dp:subject>
<dp:body>
	<dp:section>
		{{ phrase('user.emails.profile_email-new-confirm') }}:<br />
		<a href="{{ url('user_validate_email', {'id': validating_email.id, 'auth': validating_email.auth}) }}">
			{{ url('user_validate_email', {'id': validating_email.id, 'auth': validating_email.auth}) }}
		</a>
	</dp:section>
</dp:body>