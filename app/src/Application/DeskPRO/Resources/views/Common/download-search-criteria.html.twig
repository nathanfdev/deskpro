{% if id or classname %}
	{# if given an id or classname then we want a wrapper #}
	<div {% if id %}id="{{ id }}"{% endif %} class="{{ classname }}" style="display:none">
	<div class="row">
		<div class="term">
			<div class="remove">-</div>
			<div class="controls">
				<div class="type"></div>
				<div class="op"></div>
				<div class="options"></div>
			</div>
		</div>
	</div>
	<div class="type" title="{{ phrase('agent.general.choose_criteria') }}" data-rule-type="">
	</div>
{% endif %}
<div class="type" title="{{ phrase('agent.general.category') }}" data-rule-type="category">
	<div class="op">
		<select name="op" class="op">
			<option value="is">{{ phrase('agent.general.is') }}</option>
			<option value="not">{{ phrase('agent.general.is_not') }}</option>
		</select>
	</div>
	<div class="options">
		<select name="category">
			{% for cat in term_options.categories %}
				<option value="{{cat.id}}">{% if cat.depth %}{{ '--'|repeat(cat.depth) }} {%endif %}{{cat.title}}</option>
			{% endfor %}
		</select>
	</div>
</div>
<div class="type" title="{{ phrase('agent.general.number_of_downloads') }}" data-rule-type="num_downloads">
	<div class="op">
		<select name="op" class="op">
			<option value="lte">{{ phrase('agent.general.less_than') }}</option>
			<option value="gte">{{ phrase('agent.general.greater_than') }}</option>
		</select>
	</div>
	<div class="options">
		<input type="text" name="num_downloads" size="4" />
	</div>
</div>
<div class="type" title="{{ phrase('agent.general.date_created') }}" data-rule-type="date_created" data-rule-handler="DeskPRO.Agent.RuleBuilder.DateTerm">
	<div class="op">
		<select name="op" class="op">
			<option value="lte">{{ phrase('agent.general.before') }}</option>
			<option value="gte">{{ phrase('agent.general.after') }}</option>
			<option value="between">{{ phrase('agent.general.between') }}</option>
		</select>
	</div>
	<div class="options">
		{% include 'DeskPRO:Common:rulebuilder-date-options.html.twig' %}
	</div>
</div>
<div class="type" title="{{ phrase('agent.general.labels') }}" data-rule-type="label">
	<div class="op">
		<select name="op" class="op">
			<option value="is">{{ phrase('agent.general.include') }}</option>
			<option value="not">{{ phrase('agent.general.does_not_include') }}</option>
		</select>
	</div>
	<div class="options">
		<input type="text" name="label" value="" />
	</div>
</div>
{% if id or classname %}
</div>
{% endif %}