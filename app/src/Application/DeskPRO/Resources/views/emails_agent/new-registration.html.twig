{% set agent_notification_footer = 'notify' %}
<dp:subject>
	New registration: {{ person.getDisplayContact() }} {% if not person.is_agent_confirmed %}(requires validation){% endif %}
</dp:subject>
<dp:body>
New registration: {{ person.getDisplayContact() }} {% if not person.is_agent_confirmed %}(requires validation){% endif %}