{% import 'DeskPRO:emails_common:layout-macros.html.twig' as layout %}
<tr>
	<td valign="top" style="color:#949494; background-color: #FFFFFF">
		{{ layout.sep }}
		{{ footer_content|raw }}
		<p style="margin: 6px 0 0 0; padding: 0; {{ layout.standard_font() }} font-size: 12px;">
			{% if app.getSetting('core.site_name') %}
				<a href="{{ app.getSetting('core.site_name') }}" style="{{ layout.info_link_style() }}; font-size: 12px;">{{ app.getSetting('core.site_name') }}</a>
				&middot; <a href="{{ app.getSetting('core.site_url') }}" style="{{ layout.info_link_style() }} font-size: 12px;">{{ app.getSetting('core.site_url') }}</a>
			{% else %}
				<a href="{{ app.getSetting('core.deskpro_url') }}" style="{{ layout.info_link_style() }} font-size: 12px;">{{ app.getSetting('core.deskpro_url') }}</a>
			{% endif %}
		</p>
	</td>
</tr>
