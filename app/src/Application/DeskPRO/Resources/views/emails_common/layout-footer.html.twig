<tr>
	<td colspan="2" align="center" bgcolor="#0D1C3E" style="padding:12px; font-size:12px; color:#fff;">
		<a href="{{ app.getSetting('core.deskpro_url') }}" style="color:#fff; text-decoration: none;">{{ app.getSetting('core.deskpro_name') }}</a>
		{% if app.getSetting('core.site_name') %}
		| <a href="{{ app.getSetting('core.site_url') }}" style="color:#fff; text-decoration: none;">{{ app.getSetting('core.site_name') }}</a>
		{% endif %}
	</td>
</tr>