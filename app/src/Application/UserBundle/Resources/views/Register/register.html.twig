{% extends 'UserBundle::layout.html.twig' %}
{% block breadcrumb %}
	<li><a href="{{ path('user_register') }}">Register</a></li>
{% endblock %}
{% block content %}
<div
	class="dp-content-block dp-register-page with-handler"
>
	<div class="dp-box">
		<div class="dp-box-header">
			<h1>{{ phrase('user.profile.register') }}</h1>
		</div>
		<div class="dp-box-content-wrapper">
			<div class="dp-box-content dp-form">
				<form action="" method="post">
				<input type="hidden" name="process" value="1" />

					<div class="dp-form-row">
						<div class="dp-form-label">
							<label>{{ phrase('user.label_your_name') }} *</label>
						</div>
						<div class="dp-form-input">
							{{ form_widget(form.person.name) }}
						</div>
					</div>

					<div class="dp-form-row">
						<div class="dp-form-label">
							<label>{{ phrase('user.label_your_email') }} *</label>
						</div>
						<div class="dp-form-input">
							{{ form_widget(form.person.email) }}
						</div>
					</div>

					<div class="dp-form-row">
						<div class="dp-form-label">
							<label>{{ phrase('user.profile.label_password') }} *</label>
						</div>
						<div class="dp-form-input">
							{{ form_widget(form.person.password) }}
						</div>
					</div>

					<div class="dp-form-row">
						<div class="dp-form-label">
							<label>{{ phrase('user.profile.label_repeat_password') }} *</label>
						</div>
						<div class="dp-form-input">
							{{ form_widget(form.person.password2) }}
						</div>
					</div>

					<div class="dp-form-row">
						<div class="dp-form-label">
							<label>{{ phrase('user.label_captcha') }} *</label>
						</div>
						<div class="dp-form-input">
							{% include 'UserBundle:Common:recaptcha.html.twig' %}
						</div>
					</div>

					<div class="dp-footer">
						<input class="btn primary" type="submit" value="{{ phrase('user.profile.register_btn') }}" />
					</div>
				</form>
			</div>
		</div>
	</div>
</div>
{% endblock %}
