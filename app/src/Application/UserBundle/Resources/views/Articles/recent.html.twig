{% extends 'UserBundle:Articles:layout.html.twig' %}
{% block breadcrumb %}
	<li><a href="{{ path('user_articles') }}">{{ phrase('user.knowledgebase') }}</a></li>
	<li><a href="{{ path('user_articles_recent') }}">{{ phrase('user.articles.recent_section_name') }}</a></li>
{% endblock %}
{% block kb_recent %}
	<div class="with-handler"
		data-element-handler="DeskPRO.User.ElementHandler.MoreLoader"
		data-load-url="{{ path('user_articles_recent', {'page': '00000'})|replace({'00000': '{page}'}) }}"
	>
		<div class="content-list">
			<ul>
				{% include 'UserBundle:Articles:recent-items.html.twig' %}
			</ul>
		</div>
		{% if show_more %}
		<div class="content-more">
			<div class="is-not-loading">
				<a class="button" href="{{ path('user_articles_recent', {'page': page+1}) }}">{{ phrase('user.more') }}</a>
			</div>
			<div class="is-loading">
				<center><img src="{{ asset('images/loading-big-circle.gif') }}" alt="" /></center>
			</div>
		</div>
		{% endif %}
	</div>
{% endblock %}
