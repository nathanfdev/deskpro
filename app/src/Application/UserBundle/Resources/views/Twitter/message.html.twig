{% extends 'UserBundle::layout.html.twig' %}

{% block content %}
    <h4>{{ phrase('user.tickets.message') }}</h4>

    <p>{{ phrase('user.general.description') }}</p>

    {{ status.text }}
{% endblock %}
