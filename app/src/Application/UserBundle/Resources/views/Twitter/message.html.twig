{% extends 'UserBundle::layout.html.twig' %}

{% block content %}
    <h4>{{ phrase('user.portal.twitter_message') }}</h4>

    <p>{{ phrase('user.description') }}</p>

    {{ status.text }}
{% endblock %}
