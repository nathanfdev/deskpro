{% extends 'ThemeParent::layout.html.twig' %}
{% block body %}

    <div class="world">

        <section>
            <div class="column-full">
                {{ page_top() }}
            </div>
        </section>

        <section>
            <div class="column-full">
                {{ page_search_box() }}
            </div>
        </section>

        <section>
            <div class="column-full">
                {{ page_tabs() }}
            </div>
        </section>

        <div class="wrapper">
            {{ flashes() }}
            <div class="column-half-plus">
                {% block page_content %}
                    <h1>this theme's proprietary tags will appear below here</h1>
                    <h3>{{ hello_world() }}</h3>
                    <h3>{{ hello_world({"name": "Christopher!"}) }}</h3>
                    <h3>{{ hello_henry() }}</h3>
                    <section>
                        <div class="column-full">
                            {% block inner_content %}
                            {% endblock inner_content %}
                        </div>
                    </section>
                {% endblock page_content %}
            </div>

            <aside class="sidebar">
                {{ sidebar() }}
            </aside>
        </div>

    </div>

{% endblock body %}