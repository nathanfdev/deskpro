{% if new_comment_form != null %}

    <br>
    <h3>New Comment</h3>

    {{ form_start(new_comment_form) }}
    {{ form_rest(new_comment_form) }}
    <button type="submit">Save Comment</button>
    {{ form_end(new_comment_form) }}
{% endif %}