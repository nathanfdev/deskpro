{{ form_start(form) }}
<div class="feedback-form">
    <form class="main-input">
        <span>I have a:</span>
        {{ form_widget(form.category) }}
        <a href="#" class="button">Start</a>
</div>

<div class="feedback-form-expanded" style="margin-top: 50px;">

        {% if is_granted('ROLE_USER') %}
        <div>
            <a class="user-logged-in" href="#">
                <i class="fa fa-user"></i> Logged in as: <span>{{ user.name }}</span>, from email <span>{{ user.primary_email_address }}</span>
            </a>
        </div>
        {% endif %}

        <div class="bucket">
            <label>Title:</label>
            {{ form_widget(form.title, { placeholder: 'A one-sentance summary' }) }}
        </div>

        <div class="bucket">
            <label>Tell us about your Feature Request</label>
            {{ form_widget(form.content, { placeholder: 'More details...' } ) }}
        </div>

        <div class="new-ticket-attachements">
            <a class="attach-file" href="#">Click to attach a file (or just drag &amp; drop a file here)</a>

            <div class="title">Attached files:</div>
            <ul>
                <li>
                    <a href="#">Screen Shot 2014-09-30 at 6.14.15 PM.png</a><span
                            class="file-size">(245kb)</span>
                    <a class="remove-attachement" href="#"><i class="fa fa-times"></i>Remove</a>
                </li>

                <li>
                    <a href="#">a_doc_file.doc</a><span class="file-size">(84kb)</span>
                    <a class="remove-attachement" href="#"><i class="fa fa-times"></i>Remove</a>
                </li>
            </ul>
        </div>


        <div class="bucket">
            <input class="button" type="submit" value="Add your feedback">
        </div>
</div>
{{ form_rest(form) }}
{{ form_end(form) }}