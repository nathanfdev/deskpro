{% extends 'Theme::layout.html.twig' %}
{% import 'Theme:News:_macros.html.twig' as macros %}

{% block inner_content %}
    {{ news_breadcrumbs({ category: null }) }}
    <h1>News Phrase</h1>

    {{ news_posts_pretty({ show_category_link: false, page: page, count: count }) }}
    {{ news_pager({ page: page, count: count }) }}
{% endblock inner_content %}
