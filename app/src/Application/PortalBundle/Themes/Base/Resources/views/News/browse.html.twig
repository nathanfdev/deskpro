{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ page.news_breadcrumbs()|raw }}
    {{ page.news_list()|raw }}
    <h1>{{ category.realTitle }}</h1>
    {{ page.news_category_subscription()|raw }}
    {{ page.news_posts_pretty()|raw }}
    {{ page.news_pager()|raw }}
{% endblock inner_content %}
