{% if brand_setting('user.kb_subscriptions') %}
    <a href="{{ path('portal_kb_article_toggle_subscription', { slug: article.slug }) }}">
        {% if is_subscribed %}
            {{ phrase('portal.general.btn-unsubscribe') }}
        {% else %}
            {{ phrase('portal.general.btn-subscribe') }}
        {% endif %}
        <i class="fa fa-rss"></i>
    </a>
{% endif %}