{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ page.feedback_breadcrumbs()|raw }}
    {{ page.feedback({ page: pg, count: count, show_pagination: show_pagination })|raw }}
{% endblock inner_content %}
