{% extends 'Theme::layout.html.twig' %}
{% import 'Theme:Feedback:_macros.html.twig' as macros %}

{# TODO #}

{% block inner_content %}
    <div class="feedback main-feedback">
        <header class="section-header">
            <h1>{{ phrase('portal.feedback.section-title') }}</h1>
            <p>
                {{ phrase('portal.feedback.section-desc') }}
            </p>
        </header>

        {% include 'Theme:Feedback:controls.html.twig' %}

        {{ feedback_list() }}

        {% if pager.haveToPaginate %}
            {{ pagerfanta(pager) }}
        {% endif %}
    </div>
{% endblock inner_content %}
