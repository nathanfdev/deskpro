{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
<h1>{{ article.realTitle }}</h1>
	<p>Posted by <a href="#">{{ article.person.name }}</a> on {{ article.date_published | date('M j Y') }}</p>
<p>{{ article.realContent }}</p>
{% endblock inner_content %}