{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
	<div class="large-title"><h1>{{ category.title }}</h1></div>

	{{ kb_articles_small({cat: category.id}) }}
{% endblock inner_content %}