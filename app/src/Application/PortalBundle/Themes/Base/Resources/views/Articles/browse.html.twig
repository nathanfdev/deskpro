{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ knowledgebase_breadcrumbs({ category: cat }) }}
    {{ knowledgebase({category: cat}) }}
    {{ knowedgebase_articles_simple({category: cat, show_pagination: true, page: page, max_per_page: 2}) }}
{% endblock inner_content %}
