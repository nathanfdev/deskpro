{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ knowledgebase_breadcrumbs({ category: category }) }}
    {{ knowledgebase({category: category}) }}
    {{ knowledgebase_articles_simple({category: category, page: page, count: count}) }}
    {{ knowledgebase_pager({category: category, page: page, count: count}) }}
{% endblock inner_content %}
