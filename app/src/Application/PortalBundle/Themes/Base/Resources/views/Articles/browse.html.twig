{% extends 'Theme::layout.html.twig' %}

{% block inner_content %}
    {{ page.knowledgebase_breadcrumbs({ category: category })|raw }}
    {{ page.knowledgebase({category: category})|raw }}
    {{ page.knowledgebase_articles_simple({category: category, page: pg, count: count})|raw }}
    {{ page.knowledgebase_pager({category: category, page: pg, count: count})|raw }}
{% endblock inner_content %}
