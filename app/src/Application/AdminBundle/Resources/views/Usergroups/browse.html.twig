{% extends 'AdminBundle::layout.html.twig' %}
{% import 'AdminBundle:Common:design-macros.html.twig' as design %}
{% block pagebar %}
     <ul>
         <li><a href="{{ path('admin_usergroups') }}">{{ phrase('agent.usergroups') }}</a></li>
		 {% if usergroup.id %}
         	<li><a href="{{ path('admin_usergroups_edit', {'id': usergroup.id}) }}">{{ usergroup.title }}</a></li>
		 {% endif %}
         <li>{{ phrase('admin.user_groups.browse_members') }}</li>
     </ul>
{% endblock %}
{% block page %}

{% if people|length %}
<div class="check-grid item-list">
	<table width="100%">
		<thead>
			<tr>
				<th style="text-align:left">&nbsp; {{ phrase('agent.name') }}</th>
				<th style="text-align:left">&nbsp; {{ phrase('agent.organization') }}</th>
				<th style="text-align:left">&nbsp; {{ phrase('agent.email_address') }}</th>
				<th style="text-align:left">&nbsp; {{ phrase('admin.user_groups.date_registered') }}</th>
			</tr>
		</thead>
		<tbody>
			{% for person in people %}
				<tr>
					<td>{{ person.display_name }}</td>
					<td>
						{% if person.organization %}
						{% if person.organization_position %}{{ person.organization_position }}, {% endif %}{{ person.organization.name }}
						{% else %}
                            {{ phrase('agent.none') }}
						{% endif %}
					</td>
					<td>{{ person.primary_email_address }}</td>
					<td>{{ person.date_created|date('Y-m-d') }}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</div>

<center>
	{% if pageinfo.prev %}<a href="{{ path('admin_usergroups_browse', {'id': usergroup.id|default('0'), 'page': pageinfo.prev}) }}">&lt;&lt;</a>{% endif %}
	{% for p in pageinfo.pages %}
		<a href="{{ path('admin_usergroups_browse', {'id': usergroup.id|default('0'), 'page': p}) }}">{{ p }}</a>
	{% endfor %}
	{% if pageinfo.next %}<a href="{{ path('admin_usergroups_browse', {'id': usergroup.id|default('0'), 'page': pageinfo.next}) }}">&gt;&gt;</a>{% endif %}
</center>

{% else %}
	<p>
		{{ phrase('admin.user_groups.no_users') }}
	</p>
{% endif %}

{% endblock %}
