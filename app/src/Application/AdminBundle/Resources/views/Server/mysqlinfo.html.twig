{% extends 'AdminBundle::layout.html.twig' %}
{% block pagebar %}
	<ul>
		<li>{{ phrase('admin.global.mysql_info') }}</li>
	</ul>
{% endblock %}
{% block page %}
<div class="check-grid item-list">
	<table width="100%">
		<thead>
			<tr>
				<th style="text-align:left" width="50%">&nbsp; {{ phrase('admin.global.variable_name') }}</th>
				<th style="text-align:left">&nbsp; {{ phrase('admin.global.value') }}</th>
			</tr>
		</thead>
		<tbody>
			{% for name,value in mysqlinfo %}
				<tr>
					<td>{{ name }}</td>
					<td>
						<input type="text" value="{{ value }}" style="border: none; background: transparent; box-shadow: none; width: 98%; font-size: 11px; font-family: monospace; color: #000;" />
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</div>
{% endblock %}
