{% extends 'AdminBundle::layout.html.twig' %}
{% block pagebar %}
<nav>
	<ul>
		{% if not app.getConfig('twitter.agent_consumer_key') %}<li style="margin-right: 5px"><a href="{{ path('admin_twitter_apps') }}">Setup Applications</a></li>{% endif %}
		<li class="add"><a href="{{ path('admin_twitter_accounts_new', {'start': 1}) }}">{{ phrase('admin.twitter.create_new_account') }}</a></li>
	</ul>
</nav>
<ul>
	<li>Twitter Accounts</li>
</ul>
{% endblock %}
{% block page %}

<ul class="item-list">
	{% for account in accounts %}
		{% include 'AdminBundle:TwitterAccount:list-row.html.twig' %}
	{% else %}
		<li>
			{{ phrase('admin.twitter.no_accounts_defined') }}
		</li>
	{% endfor %}
</ul>

{% endblock %}
