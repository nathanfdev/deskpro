<div>
	<span class="drag_handle"><i class="icon-reorder"></i></span>
	<nav>
		<ul>
			<li><span class="opt_btm"><i class="icon-cog"></i></span></li>
			<li><span class="remove_btn"><i class="icon-remove"></i></span></li>
		</ul>
	</nav>
	<div ng-if="field.field_type == 'department'">
		<h4>Department</h4>
	</div>

	<div ng-if="field.field_type == 'category'">
		<h4>Category</h4>
	</div>

	<div ng-if="field.field_type == 'priority'">
		<h4>Priority</h4>
	</div>

	<div ng-if="field.field_type == 'workflow'">
		<h4>Workflow</h4>
	</div>

	<div ng-if="field.field_type == 'product'">
		<h4>Product</h4>
	</div>
</div>