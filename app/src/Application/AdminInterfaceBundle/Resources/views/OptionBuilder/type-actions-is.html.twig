<dp-optionbuilder-row>
	<div class="dp-ob-standard-row">
		<label class="row-label">{{ ng_var('type_title') }}</label>
		<span ng-show="rowOpts.rowEnabled">{{ ng_var('description') }}</span>
	</div>
</dp-optionbuilder-row>