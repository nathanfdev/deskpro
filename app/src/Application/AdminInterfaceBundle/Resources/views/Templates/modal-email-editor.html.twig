<div class="modal-dialog dp-codeeditor-modal">
	<div class="modal-content">
		<div class="modal-header">
			<button type="button" class="close" ng-click="dismiss()">&times;</button>
			<h4 class="modal-title">{{ ng_var('email.display_title') }}</h4>
		</div>
		<div class="modal-body">
			<p>
				{{ ng_var('email.display_description') }}
			</p>
			<div class="editor-row">
				<label>Email Subject:</label>
				<div class="editor-wrap subject">
					<div class="editor" ng-model="email.template_code.subject" ui-ace="{
						mode: 'twig',
						useWrapMode: true,
						showGutter: false,
						theme: 'textmate',
						onLoad: aceLoadedSubject
					}"></div>
				</div>
			</div>
			<div class="editor-row">
				<label>Email Body:</label>
				<div class="editor-wrap body">
					<div class="editor" style="height: 300px;" ng-model="email.template_code.body" ui-ace="{
						mode: 'twig',
						useWrapMode: true,
						theme: 'textmate',
						onLoad: aceLoadedMessage
					}"></div>
				</div>
			</div>
		</div>
		<div class="modal-footer">
			<div ng-hide="saving_template">
				<button class="btn btn-warning" ng-click="revert()" ng-show="email.is_custom"><i class="fa fa-step-backward"></i> Revert To Default</button>
				<button class="btn btn-success" ng-click="save()"><i class="fa fa-save"></i> {{ phrase('agent.general.save') }}</button>
			</div>
			<div ng-show="saving_template">
				<i class="spinner-flat"></i>
			</div>
		</div>
	</div>
</div>