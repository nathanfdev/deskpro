<div class="pane">
	<header class="pane-header">
		<h3><i class="icon-ticket"></i> {{ phrase('adm.general.apps_portal') }} &rarr; {{ phrase('agent.general.feedback') }} &rarr; {{ phrase('agent.general.types') }}</h3>
		<button class="btn help-page-trigger"><i class="icon-question-sign"></i></button>
	</header>
    <article class="pane-content no-pad">
        <div class="panel panel-fill">
            <div class="panel-heading">
                <nav class="dp-controls">
                    <ul>
                        <li>
                            <span dp-ping-flash="display_orders" class="saved-ping"><i class="icon-check-sign"></i> {{ phrase('agent.general.saved') }}</span>
                        </li>
                        <li>
                            <span class="btn btn-danger" ng-class="{active: delete_mode}" ng-click="delete_mode = !delete_mode;"><i class="icon-trash"></i> {{ phrase('agent.general.delete') }}</span>
                        </li>
                        <li>
                            <a class="btn btn-success" href="{{ ng_href('portal.feedback_types.gocreate') }}">
                                <i class="icon-plus"></i> {{ phrase('agent.general.add') }}
                            </a>
                        </li>
                    </ul>
                </nav>
                <h3 ng-pluralize count="FeedbackTypesList.feedback_types.length || 0" when="{{ ng_plural_phrase('adm.general.x_feedback_types') }}"></h3>
            </div>
            <ul class="dp-item-list" ui-sortable="FeedbackTypesList.sortedListOptions">
                <li class="with-drag-handle" ng-repeat="type in FeedbackTypesList.feedback_types" dp-state-mark="portal.feedback_types.edit.{{ ng_var('type.id') }}" data-id="{{ ng_var('type.id') }}">
                    <a href="{{ ng_href('portal.feedback_types.edit', '{id: type.id}') }}">
                        <i class="icon-reorder drag-handle"></i>
                        <div class="pull-right">
                            <span class="badge" ng-hide="delete_mode" style="margin-top: -2px;" tooltip="The ID can be used in the API" tooltip-placement="left">
                                ID: {{ ng_var('type.id') }}
                            </span>
                            <span class="btn btn-danger btn-xs only-icon" ng-show="delete_mode" style="margin-top: -3px;" ng-click="FeedbackTypesList.startDelete(type); $event.preventDefault();">
                                <i class="icon-trash"></i>
                            </span>
                        </div>
                        <h4><span class="title-text">{{ ng_var('type.title') }}</span></h4>
                    </a>
                </li>
            </ul>
        </div>
    </article>
</div>
<div dp-register-message="saved_settings">{{ phrase('adm.general.save_settings_success') }}</div>
<div dp-register-message="no_delete_last">{{ phrase('adm.feedback_types.no_delete_last') }}</div>
{% include 'AdminInterfaceBundle:FeedbackTypes:help-page.html.twig' %}