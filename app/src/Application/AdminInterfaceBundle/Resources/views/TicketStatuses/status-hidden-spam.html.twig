{% extends 'AdminInterfaceBundle:Layout:list-editpane.html.twig' %}
{% block editpane_content %}
	<section class="dp-paneblock dp-editpane">
		<article>
			<div class="panel panel-fill indent">
				<div class="panel-body">
					<p>When agents spam a ticket, it is removed from view and is held under the 'spam' status.</p>
					<p>
						You currently have <strong>{{ ng_var('getCount()') }}</strong> spam tickets.
					</p>
				</div>
			</div>
		</article>
	</section>

	<form class="form-horizontal dp-form" role="form" name="form_props" ng-submit="TicketStatusEdit.saveLabel()">
		<section class="dp-paneblock dp-editpane">
			<article class="form-horizontal dp-form">
				<div class="panel panel-fill indent">
					<div class="panel-body">
						<div class="form-group">
							<div class="col-lg-3">
								<h3>Automatic Cleanup</h3>
							</div>
							<div class="col-lg-9">
								<p>
									Spammed tickets are automatically purged from the system after
									<br/>
									<dp-time-with-unit ng-model="settings.auto_purge_time"></dp-time-with-unit>
								</p>
								<br/>
								<button class="btn btn-default" ng-click="$event.preventDefault(); TicketStatusEdit.startPurge();"><i class="fa fa-trash-o"></i> Run cleanup now</button>
							</div>
						</div>
					</div>
				</div>
			</article>
			<footer>
				<div class="dp-content">
					<div class="text-danger ng-hide" ng-show="form_props.$attempted && form_props.$invalid">
						<p><strong>{{phrase('adm.general.please_correct_errors')}}</strong></p>
						<ul>
							<li ng-show="form_props.label.$error.required">{{phrase('adm.general.label_edit_error')}}</li>
						</ul>
					</div>
					<div class="dp-btn-controls" ng-hide="form_props.$attempted && form_props.$invalid">
						<div dp-hide-spinning="saving_label">
							<button class="btn btn-primary" dp-submit-form>{{phrase('agent.general.save')}}</button>
						</div>
						<div dp-show-spinning="saving_label">
							<i class="spinner-flat"></i>
						</div>
					</div>
				</div>
			</footer>
		</section>
	</form>
	{{ ng_tpl('AdminBundleInterface:TicketStatuses:modal-purge-spam.html.twig') }}
{% endblock %}