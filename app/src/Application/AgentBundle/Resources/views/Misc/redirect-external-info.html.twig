<dl>
	{% if info.title %}
		<dt>{{ phrase('agent.redirect_page_title') }}:</dt>
		<dd>{{ info.title }}</dd>
	{% endif %}

	<dt>{{ phrase('agent.page') }}:</dt>
	<dd>{{ info.size_readable }} {{ phrase('agent.redirect_page_makeup', {'num_images': info.num_images, 'num_scripts': info.num_scripts}) }}</dd>

	<dt>{{ phrase('agent.redirect_server_ip') }}:</dt>
	<dd>{{ info.ip }}</dd>

	<dt>{{ phrase('agent.host') }}:</dt>
	<dd>{{ info.hostname }}</dd>
</dl>
