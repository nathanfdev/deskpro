<ul class="ticket-message-edit-menu" style="display:none">
	<li data-option-id="view-details">{{ phrase('agent.detail') }}</li>
	<li data-option-id="quote">{{ phrase('agent.quote') }}</li>
	{#<li data-option-id="split">{{ phrase('agent.tickets.split_from_here') }}</li>#}
</ul>
<ul class="ticket-macros-menu" style="display:none" {% if baseId %}id="{{baseId}}_macros_menu"{% endif %}>
	{% for macro in macros %}
		<li data-macro-id="{{macro.id}}">{{macro.title}}</li>
	{% endfor %}
</ul>

<div class="delete-ticket-overlay overlay" style="display:none">
	<div class="overlay-title">
		<h4>{{ phrase('agent.ucfirst_delete') }}</h4>
	</div>

	<div class="overlay-content">
		{{ phrase('agent.deleted_reason') }}: <input type="text" value="" class="delete-reason" style="width: 200px;" />
	</div>

	<div class="overlay-footer">
		<div class="loading-off">
			<button class="save-trigger clean-white">{{ phrase('agent.ucfirst_delete') }}</button>
		</div>
		<div class="loading-on" style="display:none">
			<span>{{ phrase('agent.saving') }}</span>
		</div>
	</div>
</div>
