<form action="{{ path('agent_peoplesearch_customfilter') }}">
	<ul class="property-list">
		{% if people_options.organizations %}
		<li class="ob">
			<label>{{ phrase('global.organization') }}</label>
			<div class="values">
				<select name="set_term[person_organization]" multiple="multiple">
					{% for id,title in people_options.organizations %}
						<option value="{{id}}">{{title}}</option>
					{% endfor %}
				</select>
			</div>
		</li>
		{% endif %}
		{% if people_options.usergroups %}
		<li class="ob">
			<label>{{ phrase('global.usergroup') }}</label>
			<div class="values">
				<select name="set_term[person_usergroup]" multiple="multiple">
					{% for id,title in people_options.usergroups %}
						<option value="{{id}}">{{title}}</option>
					{% endfor %}
				</select>
			</div>
		</li>
		{% endif %}
		<li class="field-input">
			<label>{{ phrase('global.label') }}</label>
			<div class="values">
				<input class="form-label-input" type="text" name="set_term[person_label]" />
			</div>
		</li>
		<li class="field-input">
			<label>{{ phrase('global.name') }}</label>
			<div class="values">
				<input type="text" name="set_term[person_name]" />
			</div>
		</li>
		<li class="field-input">
			<label>{{ phrase('global.email_address') }}</label>
			<div class="values">
				<input type="text" name="set_term[person_email]" />
			</div>
		</li>
		<li class="field-input">
			<label>{{ phrase('global.phone') }}</label>
			<div class="values">
				<input type="text" name="set_term[person_contact_phone]" />
			</div>
		</li>

		{% if ticket_options.custom_people_fields %}
			{% for field in ticket_options.custom_people_fields %}
			{% if field.field_def.getFilterCapabilities('filter') %}
			<li {% if 'Choice' in field.field_def.handler_class %}class="ob"{% else %}class="field-input"{% endif %}>
				<label>{{field.field_def.title}}</label>
				<div class="values">
					{{ render_custom_field_form(field, {field_group: 'set_custom_field'}) }}
				</div>
			</li>
			{% endif %}
			{% endfor %}
		{% endif %}
	</ul>
	<br class="clear" />

	<div class="search-form" id="peoplesearch_criteria_list" data-templates="#peoplesearch_criteria_tpl">
		<div class="search-terms"></div>
		<div class="term"><span class="add-term">+</span> {{ phrase('agent.add_criteria_term') }}</div>
	</div>
</form>

{% include 'DeskPRO:Common:people-search-criteria.html.twig' with {
	classname: 'search-builder-tpl',
	id: 'peoplesearch_criteria_tpl',
	term_options: term_options,
	no_show_fields: ['person_organization', 'person_usergroup', 'person_label', 'person_name', 'person_email', 'person_phone', 'person_field'],
	search_context: 'filter'
} %}
