<li class="cat-title-{{category.id}} category" data-category="{{ category.id }}" data-tab-for="article.cat-{{ category.id }}">
	<a>
		<span class="edit-cat-trigger"></span>
		<span class="label">{{ category.title }}</span>
	</a>
</li>
