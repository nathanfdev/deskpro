<select name="{{ name }}">
	<option value="new">{{ phrase('global.new') }}</option>
	<optgroup label="{{ phrase('global.active') }}">
		{% for cat in active_status_cats %}
			<option value="active.{{ cat.id }}">{{ cat.title }}</option>
		{% endfor %}
	</optgroup>
	<optgroup label="{{ phrase('global.closed') }}">
		{% for cat in closed_status_cats %}
			<option value="closed.{{ cat.id }}">{{ cat.title }}</option>
		{% endfor %}
	</optgroup>
	{% if constant('DP_DEBUG') %}
		<option value="hidden.validating" selected="selected">{{ phrase('global.dev_validating') }}</option>
	{% endif %}
</select>
