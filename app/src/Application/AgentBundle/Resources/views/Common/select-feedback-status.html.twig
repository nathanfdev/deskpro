<select name="{{ name }}">
	<option value="new">{{ phrase('agent.new') }}</option>
	<optgroup label="{{ phrase('agent.active') }}">
		{% for cat in active_status_cats %}
			<option value="active.{{ cat.id }}">{{ cat.title }}</option>
		{% endfor %}
	</optgroup>
	<optgroup label="{{ phrase('agent.closed') }}">
		{% for cat in closed_status_cats %}
			<option value="closed.{{ cat.id }}">{{ cat.title }}</option>
		{% endfor %}
	</optgroup>
	{% if constant('DP_DEBUG') %}
		<option value="hidden.validating" selected="selected">{{ phrase('agent.dev_validating') }}</option>
	{% endif %}
</select>
