<article class="cat-{{ category.id }}" style="display:none;">
	<ul>
		{% for snippet in snippets %}
			{% include 'AgentBundle:Common:text-snippets-row.html.twig' %}
		{% endfor %}
		<li class="new-snippet">
			<div class="display">
				<button class="clean-white add-snippet-trigger">{{ phrase('agent.add_new_snippet') }}</button>
			</div>
			<div class="input" style="display: none;">
				<form>
					<input type="hidden" name="category_id" value="{{ category.id }}" />
					<input type="hidden" name="snippet_id" value="0" />
					<input type="text" name="title" value="" placeholder="{{ phrase('global.title') }}" />
					<textarea name="snippet" placeholder="{{ phrase('agent.snippet_text_placeholder') }}"></textarea>
					<button class="clean-white save-snippet-trigger">{{ phrase('global.save') }}</button>
				</form>
			</div>
		</li>
	</ul>
</article>
