{% import 'AgentBundle:Common:agent-macros.html.twig' as agentui %}
<script>
pageMeta.fragmentClass = 'DeskPRO.Agent.PageFragment.ListPane.PublishSearchLog';

{% set baseId = el_uid() %}
pageMeta.baseId = '{{ baseId }}';
</script>
{{ agentui.scroll_containers() }}

<header class="list-grouping-bar">
	<ul id="{{ baseId }}_tabs">
		<li data-tab-for="#{{ baseId }}_recentsearches" class="on">
			<a>{{ phrase('agent.publish.searches_recent') }}</a>
		</li>
		<li data-tab-for="#{{ baseId }}_popularsearches">
			<a>{{ phrase('agent.publish.searches_popular') }}</a>
		</li>
		<li data-tab-for="#{{ baseId }}_popularlowsearches">
			<a>{{ phrase('agent.publish.searches_popular_low') }}</a>
		</li>
	</ul>
	<br style="clear: both; height: 1px; overflow: hidden; "/>
</header>

<div id="{{ baseId }}_recentsearches" style="display: none;">
	<div class="profile-box-container">
		<header>
			<h4>{{ phrase('agent.publish.searches_last_100') }}</h4>
		</header>
		<section>
			<article>
				<table class="tickets-simple" width="100%" border="0" cellpadding="0" cellspacing="0">
					<thead>
						<tr>
							<th>{{ phrase('agent.search_terms') }}</th>
							<th width="100">{{ phrase('agent.matches') }}</th>
						</tr>
					</thead>
					<tbody>
						{% for search in recent_searches %}
							<tr class="line-1">
								<td>{{ search.query }}</td>
								<td>{{ search.num_results }}</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</article>
		</section>
	</div>
</div>

<div id="{{ baseId }}_popularsearches" style="display: none;">
	<div class="profile-box-container">
		<header>
			<h4>{{ phrase('agent.publish.searches_most_popular_terms') }}</h4>
		</header>
		<section>
			<article>
				<table class="tickets-simple" width="100%" border="0" cellpadding="0" cellspacing="0">
					<thead>
						<tr>
							<th>{{ phrase('agent.search_terms') }}</th>
							<th width="100">{{ phrase('agent.frequency') }}</th>
							<th width="100">{{ phrase('agent.matches') }}</th>
						</tr>
					</thead>
					<tbody>
						{% for search in popular_searches %}
							<tr class="line-1">
								<td>{{ search.query }}</td>
								<td>{{ search.num_searches }}</td>
								<td>{{ search.num_results }}</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</article>
		</section>
	</div>
</div>

<div id="{{ baseId }}_popularlowsearches" style="display: none;">
	<div class="profile-box-container">
		<header>
			<h4>{{ phrase('agent.publish.searches_most_popular_low') }}</h4>
		</header>
		<section>
			<article>
				<table class="tickets-simple" width="100%" border="0" cellpadding="0" cellspacing="0">
					<thead>
						<tr>
							<th>{{ phrase('agent.search_terms') }}</th>
							<th width="100">{{ phrase('agent.frequency') }}</th>
							<th width="100">{{ phrase('agent.matches') }}</th>
						</tr>
					</thead>
					<tbody>
						{% for search in popular_searches_low %}
							<tr class="line-1">
								<td>{{ search.query }}</td>
								<td>{{ search.num_searches }}</td>
								<td>{{ search.num_results }}</td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
			</article>
		</section>
	</div>
</div>

{{ agentui.scroll_containers_end() }}
