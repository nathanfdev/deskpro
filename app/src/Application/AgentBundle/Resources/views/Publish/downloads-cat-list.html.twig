{% for cat in cats %}
	<li
		class="download-cat-{{ cat.id }} {% if cat.children|length %}has-children{% endif %}"
		data-category-id="{{ cat.id }}"
		data-usergroup-ids="{{ download_cats_usergroups[cat.id]|default([])|join(',') }}"
	>
		<span class="edit-cat">{{ phrase('agent.general.edit') }}</span>
		<span class="delete-cat" {% if counts[cat.id] %}style="display: none"{% endif %}></span>
		<span class="sub-toggle">{{ phrase('agent.general.expand') }}</span>
		<span class="title is-nav-item fitted" data-route="listpane:{{ path('agent_downloads_list', {'category_id':cat.id}) }}">
			<span class="list-counter"
				id="download_cat_count_{{ cat.id }}"
				data-count="{{ counts[cat.id]|default(0) }}"
			>{{ counts[cat.id]|default(0) }}</span>
			<span class="title-edit">{{ cat.title }}</span>
			<em class="spacer">&nbsp;</em>
		</span>
		<ul class="sub-group">
			{% if download_repo.getChildren(cat)|length %}
				{% include 'AgentBundle:Publish:downloads-cat-list.html.twig' with {'cats': download_repo.getChildren(cat), 'counts': counts, 'depth': depth+1 } %}
			{% endif %}
		</ul>
	</li>
{% endfor %}
