{% extends 'AgentBundle:Login:layout.html.twig' %}
{% block head %}
<script type="text/javascript">
	$(document).ready(function() {
		$('#form').on('submit', function(ev) {
			if ($('#new_password').val().length < 4) {
				alert('Please enter a password of at least 4 characters.');
				ev.preventDefault();
				return;
			}
			if ($('#new_password').val() != $('#new_password2').val()) {
				alert('The two password fields do not match.');
				ev.preventDefault();
			}
		});
	});
</script>
{% endblock %}
{% block content %}
<form action="{{ path('agent_login', {reset_code: reset_code}) }}" id="form" method="post">

<h1 id="login_title">{{ phrase('agent.login.enter_new_password') }}</h1>

<dl>
	<dt>{{ phrase('agent.global.new_password') }}</dt>
	<dd><input type="password" class="text" value="" name="new_password" id="new_password" size="40" tabindex="1" /></dd>
</dl>

<div id="login_fields">
	<dl>
		<dt>{{ phrase('agent.global.repeat') }}</dt>
		<dd><input type="password" class="text" value="" name="new_password2" id="new_password2" size="40" tabindex="1" /></dd>
	</dl>

	<dl>
		<dt></dt>
		<dd class="btn"><button id="submit_btn">{{ phrase('agent.global.reset_password') }}</button></dd>
	</dl>
</div>
</form>
{% endblock %}