<table class="list" cellspacing="0" width="100%">
	<thead>
		<tr>
			<th>{{ phrase('agent.global.name') }}</th>
			<th>{{ phrase('agent.global.email') }}</th>
			<th>{{ phrase('agent.global.labels') }}</th>
		</tr>
	</thead>
	<tbody>
		{% for person in people_list %}
			<tr data-route="person:{{ path('agent_people_view', {'person_id': person.id}) }}">
				<td>{{person.display_name}}</td>
				<td>{{person.primary_email.email|default(phrase('agent.peoplesearch.no_email'))}}</td>
				<td>{{ phrase('agent.global.labels') }}</td>
			</tr>
		{% else %}
			<tr><td colspan="3">{{ phrase('agent.peoplesearch.no_people_match_your_criteria') }}</td></tr>
		{% endfor %}
	</tbody>
</table>