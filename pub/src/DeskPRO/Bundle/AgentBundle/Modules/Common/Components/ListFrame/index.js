import { ListFrameContainer } from './frame';
import ListFrameContents from '../ListFrameContents';
import { ControlBar } from './ControlBar/ControlBar';
import { TableView, TableBody, TableHeader, Th, Row, Td, IdContainer, PersonInTable, TableCheckbox } from './TableView';
import { FilterBy } from './FilterBy';

export default {
  TableView, TableBody, TableHeader, Th, Row, Td, IdContainer, PersonInTable, TableCheckbox,
  ListFrameContainer,
  ListFrameContents,
  ControlBar,

  // remove
  FilterBy
};
