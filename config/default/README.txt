These are the default config templates.

During build they will be moved up one directory and they will be
the actual files that customers edit.

As a developer, you can copy this directory to a directory named
"dev/" and edit them as needed.